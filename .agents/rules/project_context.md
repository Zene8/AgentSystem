# Project Context

## Tech Stack
[List: frameworks, databases, key libraries relevant to this project]

## Architecture Decisions
[Key decisions made: monolith vs. microservices, caching strategy, auth approach, etc.]

## Known Constraints
[Things agents should know: performance budget, deprecated libraries, temporary workarounds]

## Where Things Live
- API endpoints: `src/api/`
- Database schema: `db/migrations/`
- Tests: `tests/` (mirror source structure)
- Configuration: `.env` (never commit secrets)
