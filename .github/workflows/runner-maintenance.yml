# runner-maintenance.yml — hands-on maintenance for the self-hosted runner's host state, from a
# dispatch instead of a shell.
#
# ── Why this workflow exists ─────────────────────────────────────────────────────────────────
#
# `ssh 100.82.195.75` is refused (port 22, Connection refused), so there is no interactive path to
# the box that holds ~/agent-memory, ~/.claude and the merge gate. The runner does execute every
# job dispatched to it, which is a remote-execution channel this repo already trusts and is the
# only one available. This file is that channel, deliberately narrowed.
#
# ── Why the input is `type: choice` and never a string ───────────────────────────────────────
#
# Every step below runs privileged commands on the host that also runs sam-audit.yml, the hard
# gate on merges to main. A free-form string input would be arbitrary remote code execution on the
# machine that decides what gets merged: anyone able to dispatch a workflow could run anything as
# the runner user. A closed choice list means the only thing a dispatcher chooses is WHICH of
# three reviewed scripts runs, and the scripts themselves are reviewed in this file.
#
# ── Why there is no `schedule:` ──────────────────────────────────────────────────────────────
#
# workflow_dispatch ONLY. config/routines.yml requires every `mechanism: cron` routine to have a
# matching job in scheduled-tasks.yml with the same cron expression, and `node tools/routines.js
# verify` exits 1 on a mismatch. A dispatch-only maintenance workflow is not a routine and must
# not quietly become one. Repair is also not a thing that should happen unattended — a human
# reads `status` first and decides.
#
# ── Why there is no `paths:` filter ──────────────────────────────────────────────────────────
#
# Nothing here is or could become a required status check, and #316 is the reason the question is
# even worth answering: an ABSENT check is not a failing one — GitHub leaves it permanently
# pending, the PR becomes unmergeable, and `enforce_admins: true` leaves no override.

name: Runner Maintenance

on:
  workflow_dispatch:
    inputs:
      mode:
        description: 'What to do on the runner host'
        required: true
        default: status
        type: choice
        options:
          # Read-only. Changes nothing. Always run this first.
          - status
          # Reconcile ~/agent-memory, resolve committed conflict markers, rebuild the graphs.
          - repair-brain
          # Escalation when repair-brain refuses because the git object store itself is corrupt
          # (not a conflict-marker problem). Backs up, clones origin fresh into a side directory,
          # proves nothing local-only would be lost, only then swaps it in.
          - reclone-brain
          # One-time bootstrap of ~/agent-memory/nexus/run-log/ so weekly-trust-scores can run.
          - bootstrap-run-log
          # Re-deploy hooks + agent defs and install the memory sync timer, then prove the drift
          # is gone. Reads ~/agent-memory but never writes it and never pushes anything.
          - repair-install
          # The one case repair-install's self-heal refuses: a canon checkout that is behind
          # origin/main AND has local edits. PRESERVES those edits with `git stash push`, proves
          # the stash object exists before touching the tree, then merge --ff-only, then prints
          # the exact `git stash pop` to restore them. Never discards anything.
          - heal-canon
          # Seed the runner's action archive cache so `Set up job` stops re-downloading the same
          # action tarballs from codeload and stops getting 429'd. Touches no repo state at all.
          - seed-action-cache

# Two dispatches touching ~/agent-memory at once would produce exactly the half-merged tree this
# workflow exists to repair.
concurrency:
  group: runner-maintenance
  cancel-in-progress: false

jobs:
  # ── status ─────────────────────────────────────────────────────────────────────────────────
  # Read-only by construction. Every command here either reads or reports; none writes to the
  # brain, and none of them may fail the job — an unknown state must still be reported, because
  # "the status probe went red" tells a human nothing about the brain.
  status:
    if: inputs.mode == 'status'
    runs-on: [self-hosted, Linux]
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Report the runner's brain state
        shell: bash
        run: |
          # `set +e` first and deliberately, same reason spelled out on the repair-install BEFORE
          # step: GitHub invokes this as `bash --noprofile --norc -e -o pipefail {0}`, and a step's
          # own `set -uo pipefail` does NOT clear that -e. Without this line the FIRST git call that
          # exits non-zero ends the probe, which is exactly what happened on run 32187001323 (#457):
          # against a brain whose object database is damaged, `git diff --diff-filter=U | wc -l`
          # returned 128, pipefail propagated it, -e killed the step, and the report stopped before
          # `dirty paths`, `unmerged` and the whole canonical-checkout section -- the one thing #449
          # tells a human to read first. A diagnostic that dies in the state it exists to diagnose
          # gives a human LESS on a sick host than on a healthy one. Every value below is therefore
          # captured with its own rc and prints as `unknown` with a reason when it cannot be read.
          set +e
          set -uo pipefail
          BRAIN="$HOME/agent-memory"
          WS="$GITHUB_WORKSPACE"

          out() { printf '%s\n' "$*" | tee -a "$GITHUB_STEP_SUMMARY"; }

          # Read one value from git. Never fails; an unreadable value carries its own reason, since
          # a blank line and a healthy empty value are indistinguishable to the human reading this.
          gitval() {
            local o rc
            o="$(git -C "$BRAIN" "$@" 2>&1)"; rc=$?
            if [ $rc -ne 0 ]; then
              printf 'unknown (git %s exited %s: %s)' "$1" "$rc" "$(printf '%s' "$o" | head -1)"
            elif [ -z "$o" ]; then
              printf '<empty>'
            else
              printf '%s' "$o"
            fi
          }

          out "## Runner brain status"
          out ""
          out '```'
          out "host          $(hostname)"
          out "date (UTC)    $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          out "brain root    $BRAIN"

          if [ ! -e "$BRAIN" ]; then
            out "STATE         MISSING — $BRAIN does not exist on this host"
            out '```'
            exit 0
          fi

          if ! git -C "$BRAIN" rev-parse --git-dir >/dev/null 2>&1; then
            out "STATE         NOT A GIT CHECKOUT — brain-sync.js cannot run here"
            out '```'
            exit 0
          fi

          GITDIR="$(git -C "$BRAIN" rev-parse --absolute-git-dir 2>/dev/null)"
          BRANCH="$(git -C "$BRAIN" rev-parse --abbrev-ref HEAD 2>/dev/null)"
          out "branch        $(gitval rev-parse --abbrev-ref HEAD)"
          out "HEAD          $(gitval log -1 "--format=%h %ad %s" --date=iso)"

          # An empty HEAD plus a failing fetch plus rc 128 is not a merge conflict -- it is a damaged
          # object database, the `brain-sync-corrupt` condition (#434). Name it, because the repair
          # for it (`reclone-brain`) is a DIFFERENT job from the repair for a conflict
          # (`repair-brain`), and a human reading a truncated report cannot tell which they need.
          FSCK_OUT="$(git -C "$BRAIN" cat-file -e 'HEAD^{commit}' 2>&1)"; FSCK_RC=$?
          if [ $FSCK_RC -ne 0 ]; then
            out "OBJECT DB     DAMAGED — HEAD does not resolve to a readable commit (rc $FSCK_RC)"
            out "              $(printf '%s' "$FSCK_OUT" | head -2 | tr '\n' ' ')"
            out "              This is corruption, NOT a merge conflict: see #434. The report below"
            out "              continues; every remaining value is best-effort."
          else
            out "OBJECT DB     HEAD resolves — no corruption at HEAD"
          fi

          IN_MERGE=no; [ -f "$GITDIR/MERGE_HEAD" ] && IN_MERGE=YES
          IN_REBASE=no
          { [ -d "$GITDIR/rebase-merge" ] || [ -d "$GITDIR/rebase-apply" ]; } && IN_REBASE=YES
          out "mid-merge     $IN_MERGE"
          out "mid-rebase    $IN_REBASE"

          # Fetch is read-only with respect to the working tree — it only updates remote refs.
          if git -C "$BRAIN" fetch --quiet origin 2>/dev/null; then
            COUNTS="$(git -C "$BRAIN" rev-list --left-right --count "origin/$BRANCH...HEAD" 2>/dev/null || echo '? ?')"
            out "behind/ahead  $COUNTS   (left = on origin only, right = LOCAL ONLY, exists nowhere else)"
          else
            out "behind/ahead  fetch failed — cannot compare with origin"
          fi

          # NOT `git ... | wc -l`: with pipefail a git rc of 128 fails the whole pipeline (the #457
          # abort), and `wc -l` of no output is `0`, which reads as "clean" -- the worst possible
          # answer on a corrupt brain. Capture, check rc, then count.
          DIRTY_RAW="$(git -C "$BRAIN" status --porcelain 2>&1)"; DIRTY_RC=$?
          if [ $DIRTY_RC -ne 0 ]; then
            DIRTY="unknown (git status exited $DIRTY_RC: $(printf '%s' "$DIRTY_RAW" | head -1))"
          else
            DIRTY="$(printf '%s' "$DIRTY_RAW" | grep -c . )"
          fi
          UNMERGED_RAW="$(git -C "$BRAIN" diff --name-only --diff-filter=U 2>&1)"; UNMERGED_RC=$?
          if [ $UNMERGED_RC -ne 0 ]; then
            UNMERGED="unknown (git diff exited $UNMERGED_RC: $(printf '%s' "$UNMERGED_RAW" | head -1))"
          else
            UNMERGED="$(printf '%s' "$UNMERGED_RAW" | grep -c . )"
          fi
          out "dirty paths   $DIRTY"
          out "unmerged      $UNMERGED"
          out '```'

          out ""
          out "### Working tree (first 40 entries of --porcelain)"
          out '```'
          if [ $DIRTY_RC -ne 0 ]; then
            out "unavailable — git status exited $DIRTY_RC"
            printf '%s\n' "$DIRTY_RAW" | head -5 | tee -a "$GITHUB_STEP_SUMMARY"
          else
            printf '%s\n' "${DIRTY_RAW:-<clean>}" | head -40 | tee -a "$GITHUB_STEP_SUMMARY"
          fi
          out '```'

          out ""
          out "### Conflict markers (read-only --check; exit 1 here just means markers exist)"
          out '```'
          MARKERS="$(node "$WS/tools/resolve-brain-markers.js" --root="$BRAIN" --check 2>&1)"; MARKERS_RC=$?
          printf '%s\n' "${MARKERS:-<no output>}" | tail -60 | tee -a "$GITHUB_STEP_SUMMARY"
          out "(exit $MARKERS_RC — 1 means markers exist, which is a finding, not a probe failure)"
          out '```'

          out ""
          out "### Brains under nexus/ and whether their graph.json parses"
          out '```'
          for dir in "$BRAIN"/nexus/*/; do
            [ -d "$dir" ] || continue
            slug="$(basename "${dir%/}")"
            g="${dir%/}/graph.json"
            # shellcheck disable=SC2016  # ${...} below is a JS template literal, not shell
            if [ ! -f "$g" ]; then
              out "$slug — no graph.json"
            elif node --input-type=module -e 'import {readFileSync} from "node:fs"; const g=JSON.parse(readFileSync(process.argv[1],"utf8")); console.log(`${(g.nodes||[]).length} nodes, ${(g.edges||[]).length} edges`);' "$g" >/tmp/gs 2>/dev/null; then
              out "$slug — OK — $(cat /tmp/gs)"
            else
              out "$slug — UNPARSEABLE graph.json (this is what breaks memory-decay)"
            fi
          done
          out '```'

          out ""
          out "### run-log (weekly-trust-scores input)"
          out '```'
          if [ -d "$BRAIN/nexus/run-log" ]; then
            out "run-log       EXISTS — $(find "$BRAIN/nexus/run-log" -name '*.json' | wc -l) json file(s)"
          else
            out "run-log       MISSING — bootstrap-run-log mode will create it"
          fi
          if [ -f "$BRAIN/nexus/trust-scores.md" ]; then
            out "trust-scores  $(stat -c '%s bytes, mtime %y' "$BRAIN/nexus/trust-scores.md")"
          else
            out "trust-scores  missing"
          fi
          out '```'

          echo "status mode changed nothing."

      # The same canon diagnostics repair-install's preflight prints, minus every mutation: no
      # fetch (which would write remote refs into a checkout this mode promises not to touch), no
      # merge, no repair. `status` exists so a human can see the state BEFORE deciding to dispatch
      # a repair, and canon's state is half of what decides that — a repair-install dispatched
      # against a canon that does not exist cannot succeed, and the answer should not cost a run.
      - name: Report the canonical checkout state (read-only, no fetch)
        shell: bash
        run: |
          set +e
          set -uo pipefail
          CANON="$HOME/dev/AgentSystem"

          out() { printf '%s\n' "$*" | tee -a "$GITHUB_STEP_SUMMARY"; }

          out ""
          out "### Canonical checkout \`$CANON\` (source for the brain-sync systemd unit)"
          out '```'
          CANON_EXISTS=no
          [ -e "$CANON" ] && CANON_EXISTS=yes
          out "canon exists       $CANON_EXISTS"

          CANON_IS_GIT=no
          if [ "$CANON_EXISTS" = yes ] && git -C "$CANON" rev-parse --git-dir >/dev/null 2>&1; then
            CANON_IS_GIT=yes
          fi
          out "canon is git       $CANON_IS_GIT"

          if [ "$CANON_IS_GIT" = yes ]; then
            out "canon branch       $(git -C "$CANON" rev-parse --abbrev-ref HEAD 2>/dev/null || echo '?')"
            out "canon HEAD         $(git -C "$CANON" log -1 --format='%h %ad %s' --date=iso 2>/dev/null || echo '?')"
            out "canon dirty paths  $(git -C "$CANON" status --porcelain 2>/dev/null | wc -l | tr -d ' ')"
            out "canon origin url   $(git -C "$CANON" remote get-url origin 2>/dev/null || echo '<no origin remote>')"
          fi

          if [ -f "$CANON/tools/brain-sync-run.js" ]; then
            out "brain-sync-run.js  present"
          else
            out "brain-sync-run.js  ABSENT — repair-install will refuse until this is true"
            out "                   (added by PR #341; absent usually means canon predates it)"
          fi
          out '```'
          out ""
          out "_No fetch was run against the canonical checkout: \`status\` mutates nothing, and even a"
          out "fetch writes remote refs. Dispatch \`repair-install\` for the bounded self-heal — fetch, a"
          out "return to \`main\` if canon is parked on another branch, then merge --ff-only; clean tree and"
          out "attached HEAD only, and the branch it leaves is never deleted._"

          # ── the actual canon diff, not just the path names (#462) ────────────────────────────
          # enforcement-drift-check reports `FAILED: canon checkout is behind origin/main`, and
          # repair-install's self-heal correctly REFUSES to rewrite a dirty tree from a dispatch.
          # That leaves exactly one way forward: a human decides keep-or-discard on those edits.
          # Until now this report printed the FILE NAMES only, and `ssh` to this host is refused,
          # so there was no way to read them at all — which is why 2 dirty paths have sat in canon
          # since 2026-08-10 failing a nightly job nobody could act on. Read-only by construction:
          # `diff`/`diff --stat`/`ls-files` compare the index and worktree against the local HEAD,
          # no fetch, no remote rev-spec, nothing written. `status` still mutates nothing.
          if [ "$CANON_IS_GIT" = yes ]; then
            CANON_DIFF_CAP=500
            DIFF_FILE="$(mktemp)"

            out ""
            out "#### canon tracked-file diff vs \`HEAD\` (read-only, no fetch)"
            git -C "$CANON" diff --stat > "$DIFF_FILE" 2>&1
            out '```'
            if [ -s "$DIFF_FILE" ]; then
              tee -a "$GITHUB_STEP_SUMMARY" < "$DIFF_FILE"
            else
              out "(no tracked-file modifications)"
            fi
            out '```'

            git -C "$CANON" diff > "$DIFF_FILE" 2>&1
            DIFF_LINES="$(wc -l < "$DIFF_FILE" | tr -d ' ')"
            if [ "$DIFF_LINES" -gt 0 ]; then
              out ""
              out "<details><summary>full <code>git diff</code> — $DIFF_LINES line(s)</summary>"
              out ""
              out '```diff'
              head -"$CANON_DIFF_CAP" "$DIFF_FILE" | tee -a "$GITHUB_STEP_SUMMARY"
              out '```'
              if [ "$DIFF_LINES" -gt "$CANON_DIFF_CAP" ]; then
                out ""
                out "_TRUNCATED at the ${CANON_DIFF_CAP}-line cap; ${DIFF_LINES} line(s) total. The"
                out "remainder is not printed here. Dispatch \`heal-canon\`: it stashes these edits"
                out "whole — every line preserved — and reports the stash ref to read them from._"
              fi
              out "</details>"
            fi

            # `git diff` shows tracked modifications only. Staged changes and untracked files are
            # invisible to it and are exactly the two things a keep-or-discard call must not be
            # told about by omission, so each is named separately when present.
            git -C "$CANON" diff --cached --stat > "$DIFF_FILE" 2>&1
            if [ -s "$DIFF_FILE" ]; then
              out ""
              out "#### canon STAGED changes (\`git diff --cached --stat\`)"
              out '```'
              tee -a "$GITHUB_STEP_SUMMARY" < "$DIFF_FILE"
              out '```'
            fi

            git -C "$CANON" ls-files --others --exclude-standard > "$DIFF_FILE" 2>&1
            if [ -s "$DIFF_FILE" ]; then
              out ""
              out "#### canon UNTRACKED files (covered by no diff above; \`heal-canon\` refuses while any exist)"
              out '```'
              head -50 "$DIFF_FILE" | tee -a "$GITHUB_STEP_SUMMARY"
              out '```'
            fi
          fi

          echo "canon report changed nothing."

      # ── watchdog liveness (#362) ─────────────────────────────────────────────────────────────
      #
      # repair-install run 31341153954 PASSED — `WATCHDOG_RC: 0`, both ExecStart scripts in sync,
      # `active actions-watchdog.timer` — and enforcement-drift-check run 31341237501, two minutes
      # later, still reported "no actions-watchdog heartbeat found". Units correct, timer active, no
      # heartbeat. `install-actions-watchdog.sh --check` cannot tell those apart because it only
      # inspects units, `gh` and timer state; it never asks whether the SERVICE ran or what happened
      # when it did. A repair whose verification cannot distinguish "installed" from "working" is a
      # paper check — the same shape as #197 and #326.
      #
      # Three faults produce that evidence and want three different human actions:
      #   (a) the service RAN and FAILED — `gh` unauthenticated for the timer's user, a node path
      #       that does not resolve in the unit's minimal environment, or a crash. `Result` and the
      #       journal say which.
      #   (b) the service NEVER TRIGGERED — `list-timers` shows LAST=n/a and ActiveEnterTimestamp
      #       is empty.
      #   (c) the heartbeat IS being written, somewhere --check-heartbeat does not look — the unit
      #       carries HOME/AGENT_MEMORY_ROOT differing from this job's. The resolved path plus its
      #       mtime settle it.
      #
      # Read-only, like the rest of `status`: nothing here fetches, installs, enables, or writes.
      # `--print-heartbeat-path` and `--check-heartbeat` are both read-only paths of the watchdog,
      # and the bare `node tools/actions-watchdog.js` — which WOULD call GitHub and could raise an
      # issue — is deliberately not run. Every command is guarded with an explicit rc so a host with
      # no systemd, no journal or no gh reports "unavailable" and the diagnosis continues, rather
      # than dying on the first probe and telling a human nothing (same `set +e` pattern as the
      # repair-install preflight).
      - name: Report actions-watchdog liveness (read-only)
        shell: bash
        run: |
          set +e
          set -uo pipefail
          WS="$GITHUB_WORKSPACE"
          NAME=actions-watchdog

          out() { printf '%s\n' "$*" | tee -a "$GITHUB_STEP_SUMMARY"; }

          # Zene8/AgentSystem is PUBLIC, so Actions logs and $GITHUB_STEP_SUMMARY are world-readable.
          # The bar for anything printed here is therefore "could this EVER carry a credential",
          # not "do I trust the person who dispatched it". Every emitter below goes through this,
          # not just `gh auth status`: today no path reaches a token (the unit gets only PATH and
          # HOME — see install-actions-watchdog.sh, which withholds GH_TOKEN on purpose — and `gh`
          # answers a bad token with `HTTP 401: Bad credentials`), but that is a guarantee resting
          # on gh's error-message behaviour rather than on this file. Two of the emitters print
          # third-party text verbatim: journalctl carries the watchdog's own
          # `cannot reach GitHub — <gh stderr>`, and the heartbeat's `reason` field newly
          # interpolates that same gh stderr (#362). One filter is cheaper than re-deriving that
          # argument the next time either of them changes.
          # The second rule drops a whole `Token:` line whatever follows it, so an unknown future
          # token prefix is still caught after the char-class rule misses it.
          redact() { sed -E 's/(gh[pousr]_|github_pat_)[A-Za-z0-9_-]+/<REDACTED>/g; s/(Token:).*/\1 <REDACTED>/'; }

          # Runs a command, captures BOTH streams, and never lets its exit status escape. systemd
          # and journalctl write the interesting part (bus unreachable, unit not loaded) to stderr.
          show() {
            local o
            o="$("$@" 2>&1)"; local rc=$?
            printf '%s\n' "${o:-<no output>}" | redact | tee -a "$GITHUB_STEP_SUMMARY"
            printf '%s\n' "  (exit $rc)" | tee -a "$GITHUB_STEP_SUMMARY"
          }

          # The runner service context has no bus exported. Same probe the repair-install steps use;
          # without it every systemctl --user call below fails with "Failed to connect to bus" and
          # the whole diagnosis reads as "systemd missing" on a host where it is fine.
          UID_N="$(id -u)"
          if [ -S "/run/user/$UID_N/bus" ]; then
            export XDG_RUNTIME_DIR="/run/user/$UID_N"
            export DBUS_SESSION_BUS_ADDRESS="unix:path=/run/user/$UID_N/bus"
            BUS="/run/user/$UID_N/bus (exported)"
          else
            BUS="/run/user/$UID_N/bus ABSENT — the user manager is not running for $(whoami); no --user unit can fire"
          fi

          out ""
          out "### actions-watchdog liveness (#362) — did the service RUN, and what happened?"
          out '```'
          out "user          $(whoami)  (uid $UID_N)"
          out "HOME          ${HOME:-<unset>}"
          out "user bus      $BUS"
          out "AGENT_MEMORY_ROOT  ${AGENT_MEMORY_ROOT:-<unset — falls back to \$HOME/agent-memory>}"
          out '```'

          out ""
          out "#### timer schedule — \`systemctl --user list-timers $NAME.timer\`"
          out "_LAST=\`n/a\` with NEXT set is fault (b): enabled, never triggered._"
          out '```'
          show systemctl --user list-timers "$NAME.timer" --all --no-pager
          out '```'

          out ""
          out "#### last service run — \`systemctl --user show $NAME.service\`"
          out "_\`Result=exit-code\` with a nonzero \`ExecMainStatus\` is fault (a): it ran and died._"
          out "_An EMPTY \`ActiveEnterTimestamp\` means the service has never been started at all._"
          out '```'
          show systemctl --user show "$NAME.service" \
            -p Result -p ExecMainStatus -p ExecMainCode -p NRestarts \
            -p ActiveEnterTimestamp -p InactiveExitTimestamp -p LoadState -p UnitFileState
          out '```'

          out ""
          out "#### journal — last 40 lines of \`journalctl --user -u $NAME.service\`"
          out "_This is where the watchdog's own stderr lands: \`cannot reach GitHub — …\` names the_"
          out "_gh failure directly. \`No journal files\` usually means no persistent journal for this_"
          out "_user, not that the service is silent._"
          out '```'
          show journalctl --user -u "$NAME.service" --no-pager -n 40
          out '```'

          out ""
          out "#### heartbeat file — resolved by the watchdog itself, not reconstructed here"
          out '```'
          NODE_BIN="$(command -v node 2>/dev/null || echo /usr/bin/node)"
          HB="$("$NODE_BIN" "$WS/tools/actions-watchdog.js" --print-heartbeat-path 2>&1)"
          HB_RC=$?
          if [ "$HB_RC" -ne 0 ]; then
            out "heartbeat path  COULD NOT RESOLVE (node exit $HB_RC): $HB"
          else
            out "heartbeat path  $HB"
            if [ -f "$HB" ]; then
              out "exists          yes"
              out "mtime/size      $(stat -c '%s bytes, mtime %y' "$HB" 2>/dev/null || echo '<stat unavailable>')"
              out "contents:"
              # Redacted like everything else: this file's `reason` field now carries gh stderr.
              redact < "$HB" 2>&1 | head -20 | tee -a "$GITHUB_STEP_SUMMARY"
            else
              out "exists          NO — nothing has ever stamped this exact path"
              # Fault (c) discriminator: a heartbeat for ANOTHER host name, or under another root,
              # means the file is being written where --check-heartbeat does not read.
              out ""
              out "any heartbeat file anywhere under \$HOME/agent-memory/nexus:"
              find "$HOME/agent-memory/nexus" -maxdepth 1 -name 'actions-watchdog-heartbeat-*.json' \
                -printf '  %p  (mtime %t)\n' 2>&1 | head -10 | tee -a "$GITHUB_STEP_SUMMARY"
            fi
          fi
          out '```'

          out ""
          out "#### \`--check-heartbeat\` verdict (read-only; this is what enforcement-drift-check asserts)"
          out '```'
          show "$NODE_BIN" "$WS/tools/actions-watchdog.js" --check-heartbeat
          out '```'

          out ""
          out "#### gh auth — REDACTED, status only"
          out "_Both watchdogs shell out to \`gh\`. Unauthenticated for THIS user is fault (a): the_"
          out "_timer fires hourly and every run dies in \`probe()\`. Note the unit runs with only PATH_"
          out "_and HOME, so a token present in this job's environment is NOT proof the unit has one._"
          out '```'
          if ! command -v gh >/dev/null 2>&1; then
            out "gh              NOT ON PATH for $(whoami)"
          else
            out "gh binary       $(command -v gh)"
            # --show-token is never passed, and the output is filtered anyway: gh prints the token
            # already masked, but a future gh (or a stray GH_TOKEN echo in an error path) must not be
            # able to put a credential into a workflow log that is public on this repo.
            gh auth status 2>&1 | redact | head -20 | tee -a "$GITHUB_STEP_SUMMARY"
          fi
          out '```'

          out ""
          out "_Read-only: this step ran no fetch, no install, no enable and wrote nothing. It did not_"
          out "_run the watchdog's live check either, which would call GitHub and could raise an issue._"

          echo "watchdog liveness report changed nothing."

  # ── repair-brain ───────────────────────────────────────────────────────────────────────────
  #
  # The dangerous one. The runner's ~/agent-memory may hold commits that exist on NO other host —
  # per-agent decision logs, user preference nodes — and may be sitting mid-merge. So:
  #
  #   * tar the whole checkout INCLUDING .git before touching anything, so local commits are
  #     recoverable even if every later step is wrong;
  #   * resolve markers BEFORE any `git add`. That inversion is the entire fix: brain-sync.js runs
  #     `git add -A` before it pulls, so a half-merged tree is just file content to it and markers
  #     get committed and pushed as data (#344, 128 files);
  #   * reconcile with `git merge`, which keeps BOTH parents. A merge commit cannot lose a local
  #     commit; a reset can;
  #   * verify zero markers remain BEFORE pushing, so this run cannot be the one that publishes
  #     markers back to origin;
  #   * on anything that cannot be reconciled losslessly, STOP — raise a human-needed alert and
  #     exit nonzero. Forcing a resolution is the failure mode, not the fix.
  #
  # Banned outright, and absent from this file on purpose: `reset --hard`, `push --force`,
  # `checkout --theirs`, `clean -fd`. Every one of them can delete a fact that exists nowhere else.
  repair-brain:
    if: inputs.mode == 'repair-brain'
    runs-on: [self-hosted, Linux]
    permissions:
      issues: write
      contents: read
    env:
      GH_REPO: ${{ github.repository }}
      ALERT_KEY: runner-brain-repair-blocked
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Back up the whole brain checkout (including .git)
        shell: bash
        run: |
          set -euo pipefail
          BRAIN="$HOME/agent-memory"
          if [ ! -d "$BRAIN" ]; then
            echo "::error::$BRAIN does not exist on this host — nothing to repair"
            exit 1
          fi
          mkdir -p "$HOME/agent-memory-backups"
          STAMP="$(date -u +%Y%m%dT%H%M%SZ)"
          BACKUP="$HOME/agent-memory-backups/agent-memory-$STAMP.tar.gz"
          # .git is included deliberately. The local-only commits are the irreplaceable part, and
          # a backup of the working tree alone would not restore them.
          tar -czf "$BACKUP" -C "$HOME" agent-memory
          echo "BACKUP=$BACKUP" >> "$GITHUB_ENV"
          echo "backup written to $BACKUP ($(du -h "$BACKUP" | cut -f1))"
          # shellcheck disable=SC2012  # names are ours (agent-memory-*.tar.gz); we want mtime order
          ls -1t "$HOME/agent-memory-backups" | head -5

      - name: Reconcile, resolve markers, rebuild every graph
        shell: bash
        run: |
          set -uo pipefail
          BRAIN="$HOME/agent-memory"
          WS="$GITHUB_WORKSPACE"
          RESOLVER="$WS/tools/resolve-brain-markers.js"
          IDENT=(-c user.name='AgentSystem runner-maintenance' -c user.email='runner-maintenance@localhost')

          stop() {
            echo "::error::$1"
            echo "STOPPING. Nothing was force-resolved and nothing was pushed."
            echo "The brain is exactly as this step found it, apart from the marker repairs listed above."
            echo "Full pre-repair backup (working tree AND .git): ${BACKUP:-<not taken>}"
            exit 1
          }

          git -C "$BRAIN" rev-parse --git-dir >/dev/null 2>&1 || stop "$BRAIN is not a git checkout"
          GITDIR="$(git -C "$BRAIN" rev-parse --absolute-git-dir)"
          BRANCH="$(git -C "$BRAIN" rev-parse --abbrev-ref HEAD)"
          [ "$BRANCH" != "HEAD" ] || stop "the brain is on a detached HEAD — a human must decide what that state means"
          if [ -d "$GITDIR/rebase-merge" ] || [ -d "$GITDIR/rebase-apply" ]; then
            stop "a rebase is in progress in $BRAIN — this job will not rewrite history it did not start"
          fi

          rebuild_graphs() {
            local dir slug rc=0
            for dir in "$BRAIN"/nexus/*/; do
              [ -d "$dir" ] || continue
              dir="${dir%/}"
              slug="$(basename "$dir")"
              { [ -d "$dir/nodes" ] || [ -f "$dir/graph.json" ]; } || continue
              echo "--- rebuilding graph for $slug"
              # --brain-path, NOT the positional `graph-init.js <slug> <path>` form. The positional
              # form treats the brain as a repo root, writes to nexus/<slug>/nexus/<slug>/graph.json
              # — a brain nested inside the brain — and prints a success line for work it did not
              # do (#346). The arrow in its output is relative, so the two forms are
              # indistinguishable from the log alone.
              node "$WS/tools/graph/graph-init.js" "$slug" --brain-path="$dir" || rc=1
            done
            return $rc
          }

          echo "=== 1. fetch (updates remote refs only; the working tree is untouched)"
          git -C "$BRAIN" fetch --quiet origin || stop "git fetch failed — cannot reconcile without origin"

          if [ -f "$GITDIR/MERGE_HEAD" ]; then
            echo "=== 2. the brain is MID-MERGE — completing it, which preserves both parents"
            node "$RESOLVER" --root="$BRAIN" || stop "could not resolve the in-progress merge losslessly"
            rebuild_graphs || stop "graph rebuild failed while completing the in-progress merge"
            git -C "$BRAIN" add -A
            git -C "$BRAIN" "${IDENT[@]}" commit --quiet --no-edit \
              || stop "could not commit the in-progress merge"
          else
            echo "=== 2. no merge in progress"
          fi

          echo "=== 3. resolve committed markers BEFORE any git add (the #344 inversion)"
          node "$RESOLVER" --root="$BRAIN" || stop "the marker repair could not resolve every file — see UNRESOLVED lines above"
          rebuild_graphs || stop "graph rebuild failed after the marker repair"

          if [ -n "$(git -C "$BRAIN" status --porcelain)" ]; then
            git -C "$BRAIN" add -A
            git -C "$BRAIN" "${IDENT[@]}" commit --quiet \
              -m "chore(brain): resolve conflict markers and rebuild graphs on $(hostname)" \
              || stop "could not commit the repaired tree"
            echo "committed the repaired tree"
          else
            echo "nothing to commit — the tree was already clean"
          fi

          echo "=== 4. merge origin/$BRANCH (a merge keeps both parents; local-only commits survive)"
          MERGE_RC=0
          git -C "$BRAIN" "${IDENT[@]}" merge --no-edit origin/"$BRANCH" || MERGE_RC=$?
          if [ "$MERGE_RC" -ne 0 ]; then
            echo "the merge conflicted — resolving with the lossless rules"
            if ! node "$RESOLVER" --root="$BRAIN"; then
              git -C "$BRAIN" merge --abort || true
              stop "the merge could not be resolved losslessly — aborted it and left the brain as found"
            fi
            if ! rebuild_graphs; then
              git -C "$BRAIN" merge --abort || true
              stop "graph rebuild failed mid-merge — aborted the merge and left the brain as found"
            fi
            git -C "$BRAIN" add -A
            git -C "$BRAIN" "${IDENT[@]}" commit --quiet --no-edit || stop "could not commit the merge resolution"
          fi

          echo "=== 5. prove zero markers remain BEFORE anything is pushed"
          if ! node "$RESOLVER" --root="$BRAIN" --check; then
            stop "conflict markers are STILL present after the repair — refusing to push them to origin"
          fi

          echo "=== 6. push via brain-sync.js (plain push; never --force)"
          node "$WS/tools/brain-sync.js" || stop "brain-sync failed — the repair is committed locally but not pushed"

          echo "=== 7. prove the repair by running the job that was failing"
          node "$WS/tools/memory-decay.js" --all || stop "memory-decay still fails against the repaired brain"

          echo "=== 8. push whatever the decay pass changed"
          node "$WS/tools/brain-sync.js" || stop "brain-sync failed after the decay pass"

          echo "repair-brain completed. Backup retained at ${BACKUP:-<not taken>}"

      - name: Post-repair status
        if: always()
        shell: bash
        run: |
          set -uo pipefail
          BRAIN="$HOME/agent-memory"
          git -C "$BRAIN" log --oneline -5 2>/dev/null || true
          git -C "$BRAIN" status --porcelain 2>/dev/null | head -20 || true
          node "$GITHUB_WORKSPACE/tools/resolve-brain-markers.js" --root="$BRAIN" --check --quiet || true

      - name: Clear the human-needed alert
        if: success()
        shell: bash
        run: |
          node "$GITHUB_WORKSPACE/tools/human-needed.js" resolve "$ALERT_KEY" \
            --comment "runner-maintenance repair-brain completed on $(hostname) at $(date -u +%Y-%m-%dT%H:%M:%SZ). Markers are gone, graphs rebuilt, memory-decay --all passes." \
            || echo "::warning::could not clear the human-needed alert"

      - name: Raise the human-needed alert (repair stopped)
        if: failure()
        shell: bash
        run: |
          node "$GITHUB_WORKSPACE/tools/human-needed.js" raise "$ALERT_KEY" \
            --title "[Infra]: the runner's agent-memory could not be repaired automatically" \
            --why "\`runner-maintenance.yml\` (mode repair-brain) stopped on \`$(hostname)\` rather than force a resolution. See [run ${{ github.run_id }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}). Stopping is the designed outcome when the brain cannot be reconciled losslessly — the runner may hold commits that exist on no other host, so a forced resolution would destroy facts permanently. A pre-repair backup of the whole checkout, .git included, was written to \`~/agent-memory-backups/\` on that host." \
            --action "Read the run log for the \`stop\` reason. Dispatch \`gh workflow run runner-maintenance.yml -f mode=status\` for the current state. Detached HEAD or an in-progress rebase means a human must decide what that state was meant to be. UNRESOLVED lines from resolve-brain-markers.js name the exact files whose conflicts neither rule could resolve; for a graph.json that means rebuilding it from nodes/ with \`node tools/graph/graph-init.js <slug> --brain-path=\\\"\$HOME/agent-memory/nexus/<slug>\\\"\` (the --brain-path form, per #346). This issue closes itself on the next successful repair." \
            || echo "::warning::could not raise the human-needed alert"

  # ── reclone-brain ──────────────────────────────────────────────────────────────────────────
  #
  # repair-brain assumes the object store is readable — it fetches, merges, resolves markers.
  # None of that works when objects are missing or empty (`error: object file .../XX is empty`,
  # `fatal: bad object HEAD`). This mode does not repair; it replaces, and only after proving
  # the replacement loses nothing:
  #
  #   * tar the whole checkout INCLUDING .git first, same as repair-brain, before touching
  #     anything;
  #   * refuse to run at all unless the checkout actually looks corrupt — this is not a shortcut
  #     around repair-brain for ordinary conflicts;
  #   * clone origin fresh into a SIDE directory — the corrupt checkout is never touched by the
  #     clone itself;
  #   * prove every local branch tip and the old HEAD exist in the fresh clone, and that fsck
  #     finds no dangling commits, BEFORE swapping anything in;
  #   * swap by `mv`, never `rm -rf` — the corrupt checkout is moved aside under a
  #     `agent-memory-corrupted-<stamp>` name and kept, not deleted;
  #   * on anything that cannot be proven safe, STOP — raise a human-needed alert and exit
  #     nonzero. That ordering (backup, prove, move-aside-never-delete, swap) is the entire
  #     safety argument.
  #
  # Banned outright, same as repair-brain: `reset --hard`, `push --force`, `checkout --theirs`,
  # `clean -fd`. Also banned here specifically: `rm -rf` on anything that was ever
  # ~/agent-memory — corrupted or not, it only ever gets moved aside.
  reclone-brain:
    if: inputs.mode == 'reclone-brain'
    runs-on: [self-hosted, Linux]
    permissions:
      issues: write
      contents: read
    env:
      GH_REPO: ${{ github.repository }}
      ALERT_KEY: runner-brain-reclone-blocked
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Back up the whole brain checkout (including .git)
        shell: bash
        run: |
          set -euo pipefail
          BRAIN="$HOME/agent-memory"
          if [ ! -d "$BRAIN" ]; then
            echo "::error::$BRAIN does not exist on this host — nothing to reclone"
            exit 1
          fi
          mkdir -p "$HOME/agent-memory-backups"
          STAMP="$(date -u +%Y%m%dT%H%M%SZ)"
          BACKUP="$HOME/agent-memory-backups/agent-memory-$STAMP.tar.gz"
          tar -czf "$BACKUP" -C "$HOME" agent-memory
          echo "BACKUP=$BACKUP" >> "$GITHUB_ENV"
          echo "STAMP=$STAMP" >> "$GITHUB_ENV"
          echo "backup written to $BACKUP ($(du -h "$BACKUP" | cut -f1))"
          # shellcheck disable=SC2012  # names are ours (agent-memory-*.tar.gz); we want mtime order
          ls -1t "$HOME/agent-memory-backups" | head -5

      - name: Refuse unless the checkout actually looks corrupt
        id: detect
        shell: bash
        run: |
          set +e
          set -uo pipefail
          BRAIN="$HOME/agent-memory"

          HEAD_OK=1
          git -C "$BRAIN" rev-parse HEAD >/dev/null 2>&1 || HEAD_OK=0
          OLD_HEAD="$(git -C "$BRAIN" rev-parse HEAD 2>/dev/null || echo unknown)"

          FSCK_OUT="$(git -C "$BRAIN" fsck --full 2>&1)"
          FSCK_RC=0
          echo "$FSCK_OUT" | grep -Eiq 'missing|empty|corrupt|error:' && FSCK_RC=1

          reasons=""
          [ "$HEAD_OK" -eq 0 ] && reasons="${reasons}HEAD does not resolve. "
          [ "$FSCK_RC" -eq 1 ] && reasons="${reasons}fsck reports damage: $(echo "$FSCK_OUT" | grep -Ei 'missing|empty|corrupt|error:' | head -5 | tr '\n' ' ')"

          if git -C "$BRAIN" fetch --quiet origin >/dev/null 2>&1; then
            echo "note: fetch against origin unexpectedly succeeded despite the above"
          fi

          echo "$reasons"

          if [ "$HEAD_OK" -eq 1 ] && [ "$FSCK_RC" -eq 0 ]; then
            echo "::error::$BRAIN does not look corrupt (HEAD resolves, fsck --full is clean). Use mode=repair-brain for ordinary conflicts, or mode=status to inspect first."
            exit 1
          fi

          echo "OLD_HEAD_OK=$HEAD_OK" >> "$GITHUB_ENV"
          echo "OLD_HEAD=$OLD_HEAD" >> "$GITHUB_ENV"
          echo "confirmed corrupt — proceeding to clone-and-prove"

      - name: Clone origin fresh, side-by-side — does not touch ~/agent-memory
        shell: bash
        run: |
          set -euo pipefail
          BRAIN="$HOME/agent-memory"
          ORIGIN_URL="$(git -C "$BRAIN" config --get remote.origin.url)"
          NEW="$HOME/agent-memory-reclone-$STAMP"
          git clone --quiet "$ORIGIN_URL" "$NEW"
          echo "NEW=$NEW" >> "$GITHUB_ENV"
          # Redact any embedded credential (https://x-access-token:TOKEN@... or a bare PAT) —
          # secret-masking only redacts values GitHub already knows are secrets, not arbitrary
          # strings baked into a git remote URL.
          SAFE_URL="$(echo "$ORIGIN_URL" | sed -E 's#://[^@/]+@#://***@#')"
          echo "cloned $SAFE_URL into $NEW"

      - name: Prove nothing local-only would be lost, then swap
        shell: bash
        run: |
          # `set +e` first and deliberately, same reason as the `status` probe (#457): GitHub
          # invokes this as `bash --noprofile --norc -e -o pipefail {0}`, and a step's own
          # `set -uo pipefail` does NOT clear that -e. Every guard below is written as
          # "run it, capture $?, then decide" -- which is a SIMPLE COMMAND, so under the
          # inherited -e a non-zero git exit killed the step before the `$?` line ever ran and
          # before `stop` could print why. That is run 32189007924 (#460): zero stdout from this
          # step, then `exit 11`, no ::error:: and no STOPPING line. The guards were correct and
          # unreachable. Nothing below relies on -e for safety: every dangerous action is
          # `... || stop` or an explicit `if !`, and `stop` is the only path that skips the swap.
          set +e
          set -uo pipefail
          BRAIN="$HOME/agent-memory"

          stop() {
            echo "::error::$1"
            echo "STOPPING. Nothing was moved and nothing was swapped."
            echo "$BRAIN is exactly as this run found it."
            echo "Fresh clone (unused) left at: ${NEW:-<none>}"
            echo "Full pre-repair backup (working tree AND .git): ${BACKUP:-<not taken>}"
            exit 1
          }

          if [ "${OLD_HEAD_OK:-0}" -eq 1 ]; then
            git -C "$NEW" cat-file -e "$OLD_HEAD" 2>/dev/null \
              || stop "old HEAD ($OLD_HEAD) does not exist in the fresh clone — it may be a local-only commit that origin never received"
          fi

          # for-each-ref on a damaged store can itself error and yield nothing — an empty loop
          # must never read as "zero branches to check". Capture it to a file and check its exit
          # code explicitly, since piping into `while read` would hide the exit code behind the
          # pipeline's last command.
          #
          # stderr goes to its OWN file, not `2>&1` into the parsed one (#460). A damaged store
          # makes for-each-ref emit lines like `error: refs/heads/main: invalid sha1 pointer
          # faf5f97…`, and merging those into the list being parsed hands the loop `sha=error:`,
          # `ref=refs/heads/main:` — a lookup that fails in the fresh clone and stops the run with
          # a message naming a branch called "error:". That is a false negative dressed as a
          # safety stop, on precisely the host this job exists for.
          REFS_FILE="$(mktemp)"
          REFS_ERR="$(mktemp)"
          git -C "$BRAIN" for-each-ref refs/heads --format='%(objectname) %(refname:short)' > "$REFS_FILE" 2> "$REFS_ERR"
          REFS_RC=$?
          REFS_LINES="$(grep -c . "$REFS_FILE")"
          if [ -s "$REFS_ERR" ]; then
            echo "for-each-ref reported problems against the corrupt store (expected; every ref it could still read is checked below):"
            head -20 "$REFS_ERR"
          fi
          # Same predicate as fsck below: the untrustworthy case is "told us nothing", not
          # "exited non-zero". A store with one broken ref makes for-each-ref exit non-zero while
          # still listing every ref it can read, and each of those IS then proven against the
          # fresh clone. Zero lines is the case that proves nothing.
          [ "$REFS_LINES" -gt 0 ] \
            || stop "for-each-ref refs/heads listed NO branches against the corrupt checkout (exit $REFS_RC) — cannot prove local branches are safe, refusing to trust an empty result"
          echo "checking $REFS_LINES local branch tip(s) against the fresh clone"
          while read -r sha ref; do
            [ -n "$sha" ] || continue
            git -C "$NEW" cat-file -e "$sha" 2>/dev/null \
              || stop "local branch $ref (at $sha) does not exist in the fresh clone — possible local-only work"
          done < "$REFS_FILE"

          # Same principle for fsck, with one correction that #460 forced. The old predicate was
          # `rc == 0 or stop`, and `git fsck` CANNOT exit 0 against a store that is corrupt by
          # definition -- so the guard demanded a clean fsck from the very repository this job
          # exists to replace, and reclone-brain could never succeed on any host that needed it.
          #
          # This is NOT "we ignore fsck failures now". The safety property is unchanged and is
          # about LOST WORK: a `dangling commit` line still stops the run, unconditionally, below.
          # What changed is the question asked of the exit code. `rc != 0` does not mean "fsck
          # told us nothing" -- against a damaged store fsck reports each bad object and keeps
          # walking, then exits non-zero precisely BECAUSE it found damage. The thing that would
          # make an empty result untrustworthy is fsck never walking at all, and that is what is
          # tested here, two ways:
          #
          #   * ZERO output lines -> it produced no enumeration whatsoever; an empty
          #     "no dangling commits" is unproven, so stop. (The original #457-class trap.)
          #   * a `fatal:` line -> git aborted mid-walk. fsck reports per-object problems as
          #     `error:` and continues; `fatal:` is the one that means it gave up, so anything it
          #     had not reached yet is unexamined and an empty dangling list is again unproven.
          #
          # Only a non-zero rc that walked far enough to report `error:`-level damage is tolerated,
          # and even then the dangling-commit check below is what actually decides.
          FSCK_FILE="$(mktemp)"
          git -C "$BRAIN" fsck --no-reflog --unreachable > "$FSCK_FILE" 2>&1
          FSCK_DANGLE_RC=$?
          FSCK_LINES="$(grep -c . "$FSCK_FILE")"
          echo "fsck --no-reflog --unreachable exited $FSCK_DANGLE_RC with $FSCK_LINES line(s) of output"
          if [ "$FSCK_DANGLE_RC" -ne 0 ]; then
            if [ "$FSCK_LINES" -eq 0 ]; then
              stop "fsck --unreachable exited $FSCK_DANGLE_RC and reported NOTHING against the corrupt checkout — it never walked the object graph, so an empty dangling-commit list proves nothing; refusing to trust an empty result"
            fi
            if grep -q '^fatal:' "$FSCK_FILE"; then
              grep '^fatal:' "$FSCK_FILE" | head -5
              stop "fsck --unreachable aborted with a fatal error against the corrupt checkout — it stopped mid-walk, so objects it never reached are unexamined and an empty dangling-commit list proves nothing"
            fi
            echo "fsck exited non-zero but walked and reported — that is the expected shape for a damaged store; the dangling-commit check below is the decisive one"
            head -20 "$FSCK_FILE"
          fi
          DANGLING="$(grep '^dangling commit' "$FSCK_FILE" || true)"
          if [ -n "$DANGLING" ]; then
            echo "$DANGLING"
            stop "the corrupt checkout has dangling commits fsck can still see — these may be lost work; a human must inspect them before this can proceed"
          fi

          echo "verified: old HEAD and every local branch tip exist in the fresh clone, no dangling commits found"

          CORRUPTED="$HOME/agent-memory-corrupted-$STAMP"
          mv "$BRAIN" "$CORRUPTED" || stop "could not move $BRAIN aside — refusing to touch the fresh clone"
          if ! mv "$NEW" "$BRAIN"; then
            echo "::error::moved $BRAIN aside to $CORRUPTED but could not move the fresh clone into place."
            echo "MANUAL RECOVERY NEEDED: fresh clone is sitting at $NEW — move it to $BRAIN by hand."
            exit 1
          fi
          echo "swapped: corrupted checkout preserved at $CORRUPTED, fresh clone now live at $BRAIN"

          for dir in "$BRAIN"/nexus/*/; do
            [ -d "$dir" ] || continue
            dir="${dir%/}"
            slug="$(basename "$dir")"
            { [ -d "$dir/nodes" ] || [ -f "$dir/graph.json" ]; } || continue
            echo "--- rebuilding graph for $slug"
            node "$GITHUB_WORKSPACE/tools/graph/graph-init.js" "$slug" --brain-path="$dir" || stop "graph rebuild failed for $slug after the swap"
          done

          echo "=== proving the fresh brain is healthy"
          node "$GITHUB_WORKSPACE/tools/memory-decay.js" --all || stop "memory-decay fails against the freshly cloned brain"

          echo "reclone-brain completed. Corrupted checkout preserved at $CORRUPTED. Backup retained at ${BACKUP:-<not taken>}"

      - name: Post-reclone status
        if: always()
        shell: bash
        run: |
          set -uo pipefail
          BRAIN="$HOME/agent-memory"
          git -C "$BRAIN" log --oneline -5 2>/dev/null || true
          git -C "$BRAIN" fsck --full 2>&1 | head -20 || true

      - name: Clear the human-needed alert
        if: success()
        shell: bash
        run: |
          node "$GITHUB_WORKSPACE/tools/human-needed.js" resolve "$ALERT_KEY" \
            --comment "runner-maintenance reclone-brain completed on $(hostname) at $(date -u +%Y-%m-%dT%H:%M:%SZ). Origin was cloned fresh after proving no local-only work would be lost. The corrupted checkout is preserved at ~/agent-memory-corrupted-$STAMP, not deleted." \
            || echo "::warning::could not clear the human-needed alert"

      - name: Raise the human-needed alert (reclone stopped)
        if: failure()
        shell: bash
        run: |
          node "$GITHUB_WORKSPACE/tools/human-needed.js" raise "$ALERT_KEY" \
            --title "[Infra]: the runner's agent-memory is corrupt and could not be safely recloned" \
            --why "\`runner-maintenance.yml\` (mode reclone-brain) stopped on \`$(hostname)\` rather than force a swap. See [run ${{ github.run_id }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}). Stopping is the designed outcome when the fresh clone cannot be proven to contain everything the corrupt checkout has — a forced swap could permanently drop a commit that exists on no other host. A pre-repair backup of the whole checkout, .git included, was written to \`~/agent-memory-backups/\` on that host, and nothing at \`~/agent-memory\` was moved." \
            --action "Read the run log for the \`stop\` reason. If it names local-only branch tips or dangling commits, a human must inspect the pre-repair tarball or the still-in-place corrupt checkout by hand and decide what to cherry-pick before this can be re-dispatched. If the checkout turns out not to be corrupt after all, use mode=repair-brain or mode=status instead. This issue closes itself on the next successful reclone." \
            || echo "::warning::could not raise the human-needed alert"

  # ── bootstrap-run-log ──────────────────────────────────────────────────────────────────────
  #
  # weekly-trust-scores is red because ~/agent-memory/nexus/run-log/ has never existed on this
  # host, and compute-trust-scores.js refuses to overwrite trust-scores.md with an empty report
  # when its input is missing (#314 — no input is a broken pipeline, not an empty result). That
  # refusal is the tool working. --allow-empty is its documented one-time bootstrap escape hatch,
  # and it still refuses when trust-scores.md holds real data, so it cannot re-create the false
  # green it replaced.
  bootstrap-run-log:
    if: inputs.mode == 'bootstrap-run-log'
    runs-on: [self-hosted, Linux]
    permissions:
      issues: write
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Create the run-log directory
        shell: bash
        run: |
          set -euo pipefail
          DIR="$HOME/agent-memory/nexus/run-log"
          if [ -d "$DIR" ]; then
            echo "$DIR already exists — $(find "$DIR" -name '*.json' | wc -l) json file(s), nothing to do"
          else
            mkdir -p "$DIR"
            echo "created $DIR"
          fi
          # A .gitkeep so the directory survives the round trip through git, which does not track
          # empty directories. Without it the next clone of the brain is back to square one.
          [ -f "$DIR/.gitkeep" ] || printf '%s\n' \
            "Run logs land here, written by the dispatch job in agent-dispatch.yml." > "$DIR/.gitkeep"

      - name: Compute trust scores once with --allow-empty
        shell: bash
        run: node "$GITHUB_WORKSPACE/tools/compute-trust-scores.js" --allow-empty

      - name: Push the bootstrap into the brain repo
        shell: bash
        run: node "$GITHUB_WORKSPACE/tools/brain-sync.js"

  # ── repair-install ─────────────────────────────────────────────────────────────────────────
  #
  # The other half of #352. `enforcement-drift-check.yml` runs four --check tools daily and, on
  # 2026-08-09 (run 31337967555), reported 5 drifted hook files, 3 missing registrations, 4
  # drifted agent definitions and both brain-sync units absent. Every one of those is repaired by
  # re-running the deployer that produced them — but there is no shell on this host (see the
  # header), so the repair had no channel. This mode is that channel.
  #
  # It also installs the off-Actions watchdog timer (#361): enforcement-drift-check.yml reported
  # "actions-watchdog heartbeat missing or stale" on 2026-08-09 (run 31340529325) as the last
  # remaining drift after a PASSING repair-install, and that timer had no repair channel either —
  # for exactly the reason it exists. It must NOT become a workflow (a repo-level Actions disable
  # would silence the one detector of a repo-level Actions disable, #197), but it can be INSTALLED
  # by one, from canon, the same way the brain-sync timer already is.
  #
  # It writes to ~/.claude/{hooks,agents} and ~/.config/systemd/user ONLY. It does not write, sync,
  # commit or push ~/agent-memory — brain-sync.js is deliberately absent from this job — and it
  # proves that at the end by comparing the brain's HEAD before and after.
  #
  # ── Why two different repo roots, which is NOT a mistake ─────────────────────────────────────
  #
  # deploy-hooks.js and sync-agents.js run from $GITHUB_WORKSPACE. They copy files INTO
  # ~/.claude and register commands whose paths point inside ~/.claude, so nothing
  # workspace-relative is ever baked into the installed state, and the fresh checkout is exactly
  # the source of truth we want them reading.
  #
  # install-brain-sync-timer.sh and install-actions-watchdog.sh run from the CANONICAL checkout
  # $HOME/dev/AgentSystem instead.
  # Each derives REPO_ROOT from its own BASH_SOURCE directory and bakes it into the unit's
  # `ExecStart=` and `WorkingDirectory=`. Installing it from the runner workspace would write a
  # unit pointing at a transient path that actions/checkout rewrites on the next run — precisely
  # the stale-unit failure its own --check exists to catch ("a task pointing at a moved or deleted
  # checkout, which looks perfectly healthy in systemctl status"). If the canonical checkout is
  # missing, this job FAILS rather than falling back; a silent fallback is how you install a timer
  # that reports healthy and never runs.
  #
  # ── Why the preflight prints before it decides, and heals within a hard boundary ──────────────
  #
  # The first repair-install dispatch (run 31338981940) died in the preflight with "brain-sync-run.js
  # is missing, so the canonical checkout is not usable", and every echo describing the canon sat
  # BELOW that fatal check. So the run proved the file was absent and said nothing about WHY —
  # missing directory, not a git checkout, wrong remote, or (the likely answer) a clone predating
  # PR #341, which is what added tools/brain-sync-run.js. Three very different human actions, no
  # evidence to choose between them. Evidence first, verdict second: the diagnostics below are
  # unconditional and run before anything can exit.
  #
  # The stale-clone case is also the one this job can fix itself, so in repair-install mode ONLY it
  # does — inside a boundary chosen so it can never destroy work:
  #
  #   * ONLY when canon is a git checkout with ZERO dirty paths. A working tree a person may be
  #     mid-edit in is not something a dispatched job gets to rewrite.
  #   * ONLY `fetch` + (if needed) `checkout main` + `merge --ff-only`. `reset --hard`,
  #     `checkout --`, `clean -fd`, `stash` and every force flag are absent on purpose, exactly as
  #     in repair-brain: none of the commands used here can make a commit unreachable.
  #   * Canon may be on ANOTHER BRANCH and still be healed, because that is the state run
  #     31339872073 actually found: a clean checkout parked on `issue-348-brain-sync-merge-guard`,
  #     whose work is already squash-merged into main, missing tools/brain-sync-run.js only because
  #     the branch was cut before #341 landed. Refusing that was a paper boundary — nothing was at
  #     risk and the host stayed broken. So a clean, attached checkout is returned to `main` first
  #     (`fetch origin main`, then `checkout main`, or `checkout -B main --track origin/main` when
  #     no local `main` exists yet), then fast-forwarded as before.
  #   * The branch it left is NEVER deleted. Its ref keeps every commit on it reachable, which is
  #     exactly what makes the switch non-destructive; the commits `origin/main..HEAD` are printed
  #     BEFORE the switch so the evidence survives it and a human can `git checkout` straight back.
  #   * A DETACHED HEAD is REFUSED with its own message and never switched away from: there is no
  #     ref holding those commits, so switching would make them reachable from nothing. That is the
  #     one case where "return canon to main" would destroy work, so it is the one case excluded.
  #   * On any other state — dirty, detached, not a checkout, or an ff-only that refuses because
  #     canon has diverged — it prints precisely which and stops for a human. It does not escalate
  #     to a stronger command.
  #
  # This is CODE only. ~/agent-memory is untouched by the heal, as it is by the rest of this job.
  # `status` mode carries the same diagnostics with no fetch, no checkout and no merge at all, so a
  # human can read canon's state without dispatching a repair.
  #
  # The --check invocations, by contrast, all run from the workspace, matching
  # enforcement-drift-check.yml exactly — so a green AFTER here is a direct prediction that
  # tomorrow's daily check is green. That is safe because --check asserts the units by MEANING
  # (the ExecStart script exists on disk, SuccessExitStatus is intact, the timer repeats), not by
  # byte-equality against the path the caller happens to sit in.
  #
  # Note: sync-agents.js writes .agents/sync.log, which is tracked, so this dirties the WORKSPACE
  # checkout. That is harmless and expected — actions/checkout cleans it on the next run, and
  # nothing here commits or pushes. Don't go looking for a bug when you see it.
  repair-install:
    if: inputs.mode == 'repair-install'
    runs-on: [self-hosted, Linux]
    # Read-only token: this job makes no API writes at all. No gh pr/issue calls, no git push.
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      # The runner's service context has no PATH for node (the sibling jobs' bare `node` calls only
      # work by luck of the current install). Resolve it absolutely, same pattern as
      # enforcement-drift-check.yml.
      - name: Resolve node's absolute path for the runner service context
        id: tools
        shell: bash
        run: |
          resolve() {
            local name="$1"; shift
            local c
            for c in "$@"; do [ -x "$c" ] && { echo "$c"; return; }; done
            command -v "$name" 2>/dev/null || true
          }
          NODE_PATH=$(resolve node "$HOME/.nvm/versions/node/*/bin/node" /usr/bin/node /usr/local/bin/node)
          echo "Resolved node: $NODE_PATH"
          echo "node_path=$NODE_PATH" >> "$GITHUB_OUTPUT"

      - name: Preflight — diagnose the canonical checkout, then heal it within a hard boundary
        id: pre
        shell: bash
        run: |
          # `set +e` first and deliberately, same reason as the BEFORE step: GitHub invokes this as
          # `bash -e -o pipefail`, and under that inherited -e the FIRST probe to exit nonzero — a
          # `git -C` against a path that is not a checkout is the obvious one — would kill the step
          # mid-diagnosis. That is precisely the evidence-free failure this step was rewritten to
          # end. Every verdict below is an explicit `fatal`, never an incidental exit code.
          set +e
          set -uo pipefail
          NODE='${{ steps.tools.outputs.node_path }}'
          CANON="$HOME/dev/AgentSystem"

          out() { printf '%s\n' "$*" | tee -a "$GITHUB_STEP_SUMMARY"; }
          fatal() { echo "::error::$1"; exit 1; }

          # ── EVIDENCE ─────────────────────────────────────────────────────────────────────────
          # Unconditional and first. Nothing here may exit, so a failing preflight still leaves a
          # human enough to tell "no directory" from "stale clone" from "wrong remote".
          out "## repair-install preflight on $(hostname) at $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          out ""
          out "### canonical checkout diagnostics (printed BEFORE any verdict)"
          out '```'
          out "node               ${NODE:-<unresolved>}"
          out "workspace          $GITHUB_WORKSPACE   (source for deploy-hooks + sync-agents)"
          out "canonical repo     $CANON   (source for the systemd unit)"

          CANON_EXISTS=no
          [ -e "$CANON" ] && CANON_EXISTS=yes
          out "canon exists       $CANON_EXISTS"

          CANON_IS_GIT=no
          if [ "$CANON_EXISTS" = yes ] && git -C "$CANON" rev-parse --git-dir >/dev/null 2>&1; then
            CANON_IS_GIT=yes
          fi
          out "canon is git       $CANON_IS_GIT"

          CANON_BRANCH='?'
          CANON_DIRTY='?'
          CANON_ORIGIN='<unknown>'
          if [ "$CANON_IS_GIT" = yes ]; then
            CANON_BRANCH="$(git -C "$CANON" rev-parse --abbrev-ref HEAD 2>/dev/null || echo '?')"
            CANON_DIRTY="$(git -C "$CANON" status --porcelain 2>/dev/null | wc -l | tr -d ' ')"
            CANON_ORIGIN="$(git -C "$CANON" remote get-url origin 2>/dev/null || echo '<no origin remote>')"
            out "canon branch       $CANON_BRANCH"
            out "canon HEAD         $(git -C "$CANON" log -1 --format='%h %ad %s' --date=iso 2>/dev/null || echo '?')"
            out "canon dirty paths  $CANON_DIRTY"
            out "canon origin url   $CANON_ORIGIN"
          fi

          SYNC_RUN=absent
          [ -f "$CANON/tools/brain-sync-run.js" ] && SYNC_RUN=present
          out "brain-sync-run.js  $SYNC_RUN   (tools/brain-sync-run.js, added by PR #341)"
          out '```'

          # ── VERDICTS ─────────────────────────────────────────────────────────────────────────
          if [ -z "$NODE" ] || [ ! -x "$NODE" ]; then
            fatal "could not resolve a node binary on this runner; every step below needs one"
          fi

          if [ "$CANON_EXISTS" != yes ]; then
            fatal "$CANON does not exist on this host at all — this is not a stale clone, there is nothing there. install-brain-sync-timer.sh bakes its own directory into the unit ExecStart=, and this job will NOT fall back to the runner workspace, whose path actions/checkout rewrites every run. Clone the repo to $CANON on this host, then re-dispatch repair-install."
          fi

          if [ "$CANON_IS_GIT" != yes ]; then
            fatal "$CANON exists but is not a git checkout, so this job cannot update it and will not guess what it is. Inspect it on the host; if it is a stray directory, move it aside and clone the repo to $CANON, then re-dispatch repair-install."
          fi

          # ── SELF-HEAL (bounded) ──────────────────────────────────────────────────────────────
          # Clean tree only; fetch, an optional `checkout main`, then ff-only. See the job header
          # for why the boundary is drawn exactly here. No reset, no `checkout --`, no clean, no
          # stash, no force — none of the commands used below can make a commit unreachable. A
          # clean checkout parked on another branch IS returned to main, and the branch it leaves
          # is never deleted, so its commits stay reachable from that ref; the commits it holds
          # over origin/main are printed BEFORE the switch so the evidence survives it. A DETACHED
          # HEAD is refused instead, because there no ref would hold them. Code only:
          # ~/agent-memory is not involved.
          out ""
          out "### canon self-heal — fetch + (return to \`main\`) + merge --ff-only (clean tree only)"
          out '```'
          HEALED=skipped
          HEAL_WHY=""
          if [ "$CANON_DIRTY" != "0" ]; then
            HEAL_WHY="canon has $CANON_DIRTY dirty path(s); someone may be mid-edit"
            out "SKIPPED — $HEAL_WHY. Rewriting that tree from a dispatch is not acceptable."
            git -C "$CANON" status --porcelain 2>/dev/null | head -20 | tee -a "$GITHUB_STEP_SUMMARY"
          elif [ "$CANON_BRANCH" = "HEAD" ] || [ "$CANON_BRANCH" = "?" ]; then
            HEALED=refused-detached
            HEAL_WHY="canon is on a DETACHED HEAD, not a named local branch"
            out "REFUSED — $HEAL_WHY. Switching away would leave the commits at that HEAD reachable from no ref at all, and this job will never do that. On the host, either give them a branch (git -C $CANON switch -c <name>) or check main out by hand, then re-dispatch repair-install."
          else
            out "HEAD before   $(git -C "$CANON" log -1 --format='%h %ad %s' --date=iso 2>/dev/null)"
            FETCH_OUT="$(git -C "$CANON" fetch --quiet origin main 2>&1)"; FETCH_RC=$?
            [ -n "$FETCH_OUT" ] && out "$FETCH_OUT"
            if [ "$FETCH_RC" -ne 0 ]; then
              HEALED=failed
              HEAL_WHY="git fetch origin main failed in $CANON (exit $FETCH_RC) — no network, no credentials, or origin ($CANON_ORIGIN) is unreachable from this host"
              out "FAILED — $HEAL_WHY"
            else
              # Return to main first, if canon is parked elsewhere. Evidence before verdict: the
              # commits this branch holds over the freshly fetched origin/main are printed while
              # HEAD still points at them. The branch ref is left in place, so `git checkout
              # $CANON_BRANCH` on the host restores exactly this state.
              SWITCHED=no
              if [ "$CANON_BRANCH" != "main" ]; then
                out ""
                out "canon is on branch '$CANON_BRANCH', not main — returning it to main."
                out "commits on '$CANON_BRANCH' that are NOT on origin/main (kept; the branch is NOT deleted):"
                AHEAD_LOG="$(git -C "$CANON" log --oneline origin/main..HEAD 2>/dev/null | head -20)"
                if [ -n "$AHEAD_LOG" ]; then
                  printf '%s\n' "$AHEAD_LOG" | tee -a "$GITHUB_STEP_SUMMARY"
                else
                  out "  (none — '$CANON_BRANCH' holds nothing that origin/main does not)"
                fi
                if git -C "$CANON" rev-parse --verify --quiet refs/heads/main >/dev/null 2>&1; then
                  # Plain checkout: it refuses rather than discarding anything, and the tree is
                  # already known clean. -B is NOT used here — on an existing local main it would
                  # move that ref and could orphan commits.
                  CO_OUT="$(git -C "$CANON" checkout main 2>&1)"; CO_RC=$?
                else
                  # No local main at all (a clone that only ever checked out this branch), so
                  # there is no ref for -B to move; it just creates main tracking origin/main.
                  CO_OUT="$(git -C "$CANON" checkout -B main --track origin/main 2>&1)"; CO_RC=$?
                fi
                [ -n "$CO_OUT" ] && out "$CO_OUT"
                if [ "$CO_RC" -ne 0 ]; then
                  HEALED=failed
                  HEAL_WHY="git checkout main refused in $CANON (exit $CO_RC) while returning it from '$CANON_BRANCH' — NOTHING was forced and the checkout was left on '$CANON_BRANCH'"
                  out "FAILED — $HEAL_WHY"
                else
                  SWITCHED=yes
                  out "OK — switched '$CANON_BRANCH' -> main. Branch '$CANON_BRANCH' still exists and still points at the commits above."
                fi
              fi
              if [ "$HEALED" != failed ]; then
                MERGE_OUT="$(git -C "$CANON" merge --ff-only origin/main 2>&1)"; MERGE_RC=$?
                [ -n "$MERGE_OUT" ] && out "$MERGE_OUT"
                if [ "$MERGE_RC" -ne 0 ]; then
                  HEALED=failed
                  HEAL_WHY="git merge --ff-only origin/main refused (exit $MERGE_RC) — canon has commits that are not on origin/main, so it cannot fast-forward. NOTHING was forced"
                  out "FAILED — $HEAL_WHY"
                elif [ "$SWITCHED" = yes ]; then
                  HEALED=yes-switched-to-main
                  out "OK — returned to main from '$CANON_BRANCH' and fast-forwarded (or already up to date)"
                else
                  HEALED=yes
                  out "OK — fast-forwarded (or already up to date)"
                fi
              fi
            fi
            out "HEAD after    $(git -C "$CANON" log -1 --format='%h %ad %s' --date=iso 2>/dev/null)"
            out "branch after  $(git -C "$CANON" rev-parse --abbrev-ref HEAD 2>/dev/null || echo '?')"
          fi
          out "result        $HEALED"
          out '```'

          # ── RE-TEST, only now ────────────────────────────────────────────────────────────────
          # A file still missing after a SUCCESSFUL ff-only pull is a categorically different fault
          # from a stale clone, and gets its own message so nobody re-runs the same repair twice.
          if [ ! -f "$CANON/tools/brain-sync-run.js" ]; then
            if [ "$HEALED" = yes ] || [ "$HEALED" = yes-switched-to-main ]; then
              fatal "the ff-only pull of $CANON SUCCEEDED and $CANON/tools/brain-sync-run.js is STILL missing. This is NOT a stale clone — a checkout in step with origin/main would have that file (PR #341). Something else is wrong with $CANON: its origin is '$CANON_ORIGIN' and may not be this repository, or it is a sparse/partial checkout that excludes tools/. Inspect it on the host and re-clone if it is the wrong repo, then re-dispatch repair-install."
            fi
            fatal "$CANON/tools/brain-sync-run.js is missing and the self-heal could not fix it: $HEAL_WHY. install-brain-sync-timer.sh bakes its own directory into the unit ExecStart=, and this job will NOT fall back to the runner workspace: that path is rewritten by actions/checkout, and the resulting unit would point at a directory that no longer exists while still looking healthy in systemctl status. Resolve the state above on the host by hand (the heal only ever runs fetch, an optional checkout of main, and merge --ff-only, and only on a clean tree with an attached HEAD, on purpose), then re-dispatch repair-install."
          fi

          out ""
          out "brain-sync-run.js present after preflight — proceeding."
          echo "canon_root=$CANON" >> "$GITHUB_OUTPUT"
          echo "canon_healed=$HEALED" >> "$GITHUB_OUTPUT"
          exit 0

      # ── BEFORE ───────────────────────────────────────────────────────────────────────────────
      # Nothing here may fail the job. The whole point is to photograph the drift that the repair
      # is about to fix, so an exit code from a --check tool is data, not an error.
      - name: BEFORE — fingerprint ~/agent-memory and run all four checks
        shell: bash
        run: |
          # set +e first and deliberately: GitHub invokes this as `bash --noprofile --norc -e -o
          # pipefail`, and a step's own `set -uo pipefail` does NOT clear that -e. Every --check
          # below is expected to exit nonzero right now; under the inherited -e the first one would
          # kill the step and the BEFORE picture would never be printed (tests/workflow_errexit).
          set +e
          set -uo pipefail
          NODE='${{ steps.tools.outputs.node_path }}'
          WS="$GITHUB_WORKSPACE"
          BRAIN="$HOME/agent-memory"

          out() { printf '%s\n' "$*" | tee -a "$GITHUB_STEP_SUMMARY"; }
          run() { "$@" 2>&1 | tee -a "$GITHUB_STEP_SUMMARY"; return "${PIPESTATUS[0]}"; }

          # Export the user bus if it exists, so the timer check's liveness half is genuinely
          # evaluated instead of reporting "cannot reach the user systemd bus" and passing.
          UID_N="$(id -u)"
          if [ -S "/run/user/$UID_N/bus" ]; then
            export XDG_RUNTIME_DIR="/run/user/$UID_N"
            export DBUS_SESSION_BUS_ADDRESS="unix:path=/run/user/$UID_N/bus"
          fi

          out "## repair-install on $(hostname) at $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          out ""
          out "## BEFORE"
          out ""

          if git -C "$BRAIN" rev-parse HEAD >/dev/null 2>&1; then
            HEAD_B="$(git -C "$BRAIN" rev-parse HEAD)"
            DIRTY_B="$(git -C "$BRAIN" status --porcelain | wc -l | tr -d ' ')"
          else
            HEAD_B="none"
            DIRTY_B="n/a"
          fi
          echo "BRAIN_HEAD_BEFORE=$HEAD_B" >> "$GITHUB_ENV"
          echo "BRAIN_DIRTY_BEFORE=$DIRTY_B" >> "$GITHUB_ENV"
          out '```'
          out "agent-memory HEAD   $HEAD_B"
          out "agent-memory dirty  $DIRTY_B path(s)"
          out "(recorded so the AFTER step can prove this job did not touch the brain)"
          out '```'

          out ""
          out "### hooks — node tools/deploy-hooks.js --check --require-install"
          out '```'
          run "$NODE" "$WS/tools/deploy-hooks.js" --check --require-install
          out "exit $?"
          out '```'

          out ""
          out "### agents — node tools/sync-agents.js --check"
          out '```'
          run "$NODE" "$WS/tools/sync-agents.js" --check
          out "exit $?"
          out '```'

          out ""
          out "### timer — bash tools/install-brain-sync-timer.sh --check"
          out '```'
          run bash "$WS/tools/install-brain-sync-timer.sh" --check
          out "exit $?"
          out '```'

          out ""
          out "### watchdog — bash tools/install-actions-watchdog.sh --check"
          out '```'
          run bash "$WS/tools/install-actions-watchdog.sh" --check
          out "exit $?"
          out '```'

          out ""
          out "### routines — node tools/routines.js verify (INFORMATIONAL ONLY, see AFTER)"
          out '```'
          run "$NODE" "$WS/tools/routines.js" verify
          out "exit $?"
          out '```'

          echo "BEFORE recorded. Nothing has been changed yet."
          exit 0

      # ── ACTIONS ──────────────────────────────────────────────────────────────────────────────
      # Each of these four tools is idempotent: on an already-repaired host they report "no
      # changes" and exit 0, which is what makes a second dispatch of this mode a no-op.
      - name: ACTIONS — deploy hooks, sync agent defs, install the memory sync + watchdog timers
        shell: bash
        run: |
          set +e
          set -uo pipefail
          NODE='${{ steps.tools.outputs.node_path }}'
          CANON='${{ steps.pre.outputs.canon_root }}'
          WS="$GITHUB_WORKSPACE"

          out() { printf '%s\n' "$*" | tee -a "$GITHUB_STEP_SUMMARY"; }
          run() { "$@" 2>&1 | tee -a "$GITHUB_STEP_SUMMARY"; return "${PIPESTATUS[0]}"; }
          rc=0

          out ""
          out "## ACTIONS"

          out ""
          out "### 1/4 node tools/deploy-hooks.js   (source: workspace)"
          out '```'
          run "$NODE" "$WS/tools/deploy-hooks.js"
          s=$?
          out "exit $s"
          out '```'
          if [ "$s" -ne 0 ]; then
            echo "::error::deploy-hooks.js failed with exit $s — hooks were not repaired"
            rc=1
          fi

          out ""
          out "### 2/4 node tools/sync-agents.js    (source: workspace; also writes the tracked .agents/sync.log)"
          out '```'
          run "$NODE" "$WS/tools/sync-agents.js"
          s=$?
          out "exit $s"
          out '```'
          if [ "$s" -ne 0 ]; then
            echo "::error::sync-agents.js failed with exit $s — agent definitions were not repaired"
            rc=1
          fi

          out ""
          out "### 3/4 bash tools/install-brain-sync-timer.sh   (source: CANONICAL checkout, not the workspace)"
          UID_N="$(id -u)"
          USER_N="$(id -un)"
          out '```'
          out "uid / user     $UID_N / $USER_N"
          out "installing from $CANON  (its path is baked into the unit ExecStart=)"

          # loginctl enable-linger is best effort and is what starts the user manager and creates
          # /run/user/<uid> in the first place. On a service account that has never logged in
          # interactively, its absence is usually WHY the bus below is unreachable.
          if loginctl enable-linger "$USER_N" >/dev/null 2>&1; then
            out "linger         enabled (or already enabled) for $USER_N"
          else
            out "linger         could NOT be enabled — best effort, continuing"
          fi

          if [ -S "/run/user/$UID_N/bus" ]; then
            export XDG_RUNTIME_DIR="/run/user/$UID_N"
            export DBUS_SESSION_BUS_ADDRESS="unix:path=/run/user/$UID_N/bus"
            out "user bus       /run/user/$UID_N/bus is a socket — exported XDG_RUNTIME_DIR and DBUS_SESSION_BUS_ADDRESS"
          else
            out "user bus       /run/user/$UID_N/bus is ABSENT — systemctl --user will not be reachable"
          fi

          # The unit bakes in whatever `command -v node` resolves to, falling back to /usr/bin/node.
          # The runner service context has no node on PATH, so without this the ExecStart= would be
          # written against a path that may not exist: an installed unit that can never run.
          NODE_DIR="$(dirname "$NODE")"
          export PATH="$NODE_DIR:$PATH"
          out "node for unit  $(command -v node)"
          out '```'
          out '```'
          run bash "$CANON/tools/install-brain-sync-timer.sh"
          TIMER_RC=$?
          out "exit $TIMER_RC"
          out '```'
          echo "TIMER_RC=$TIMER_RC" >> "$GITHUB_ENV"
          echo "TIMER_USER=$USER_N" >> "$GITHUB_ENV"

          # Contract (tools/install-brain-sync-timer.sh header): 0 = units written AND the timer is
          # enabled and active. 4 = units written but the user bus was unreachable or `enable --now`
          # failed, so the timer is NOT running and only a human at a console can finish it.
          # Anything else = the units could not even be written.
          case "$TIMER_RC" in
            0)
              out ""
              out "timer installed and enabled. The AFTER step verifies it with --check, whose"
              out "liveness half must also pass."
              ;;
            4)
              out ""
              out "TIMER NOT RUNNING (exit 4). The units are on disk but were never enabled."
              out "The AFTER step proves the units landed with --check-units and then FAILS this job."
              ;;
            *)
              echo "::error::install-brain-sync-timer.sh exited $TIMER_RC — the units could not be written at all"
              rc=1
              ;;
          esac

          # ── 4/4 the off-Actions watchdog timer (#361) ──────────────────────────────────────
          #
          # Same CANONICAL-checkout rule as 3/4, for the same reason: install-actions-watchdog.sh
          # derives REPO_ROOT from its own BASH_SOURCE directory and bakes it into ExecStart= and
          # WorkingDirectory=. Installed from $GITHUB_WORKSPACE it would write a unit pointing at a
          # path actions/checkout rewrites on the next run — a timer that looks healthy in
          # `systemctl status` and runs a script that is not there.
          #
          # This step does NOT export GH_TOKEN, and must not: the installer's own gh guard is the
          # only thing standing between us and a timer that fires hourly against an unauthenticated
          # gh, fails hourly, and stamps no heartbeat — which is indistinguishable from #361 itself.
          # A token injected here would prove authentication the unit will never have.
          #
          # The environment set up in 3/4 (linger, the exported user bus, node on PATH) is still in
          # scope — same shell, same step — and is exactly what this installer needs too.
          out ""
          out "### 4/4 bash tools/install-actions-watchdog.sh   (source: CANONICAL checkout, not the workspace)"
          out '```'
          out "installing from $CANON  (its path is baked into the unit ExecStart=)"
          run bash "$CANON/tools/install-actions-watchdog.sh"
          WATCHDOG_RC=$?
          out "exit $WATCHDOG_RC"
          out '```'
          echo "WATCHDOG_RC=$WATCHDOG_RC" >> "$GITHUB_ENV"

          # Contract (tools/install-actions-watchdog.sh header): 0 = units written AND the timer is
          # enabled and active. 4 = units written but the user bus was unreachable or `enable --now`
          # failed. 5 = gh missing or unauthenticated, so it refused to write anything. Anything
          # else = the units could not be written.
          case "$WATCHDOG_RC" in
            0)
              out ""
              out "watchdog timer installed and enabled. The AFTER step verifies it with --check,"
              out "whose liveness half must also pass."
              ;;
            4)
              out ""
              out "WATCHDOG TIMER NOT RUNNING (exit 4). The units are on disk but were never enabled."
              out "The AFTER step proves the units landed with --check-units and then FAILS this job."
              ;;
            5)
              out ""
              out "WATCHDOG NOT INSTALLED (exit 5) — gh is missing or unauthenticated for $USER_N, so"
              out "the installer refused to write units it knows could never stamp a heartbeat."
              out "The AFTER step FAILS this job and prints the human command."
              ;;
            *)
              echo "::error::install-actions-watchdog.sh exited $WATCHDOG_RC — the units could not be written at all"
              rc=1
              ;;
          esac

          exit "$rc"

      # ── AFTER + VERDICT ──────────────────────────────────────────────────────────────────────
      #
      # Gated on the four things this mode actually repairs. `routines.js verify` runs too, but is
      # reported as INFORMATIONAL and does not gate: a routines/workflow mismatch is a repo-side
      # code change (an entry in config/routines.yml against a job in scheduled-tasks.yml), and
      # nothing this job runs on the host can fix one. Gating on it would leave repair-install
      # permanently red for a reason it is structurally unable to address, and a mode that is
      # always red is a mode nobody reads.
      - name: AFTER — re-run every check, prove the brain is untouched, then verdict
        if: always()
        shell: bash
        run: |
          set +e
          set -uo pipefail
          NODE='${{ steps.tools.outputs.node_path }}'
          WS="$GITHUB_WORKSPACE"
          BRAIN="$HOME/agent-memory"

          out() { printf '%s\n' "$*" | tee -a "$GITHUB_STEP_SUMMARY"; }
          run() { "$@" 2>&1 | tee -a "$GITHUB_STEP_SUMMARY"; return "${PIPESTATUS[0]}"; }
          gate=0

          UID_N="$(id -u)"
          if [ -S "/run/user/$UID_N/bus" ]; then
            export XDG_RUNTIME_DIR="/run/user/$UID_N"
            export DBUS_SESSION_BUS_ADDRESS="unix:path=/run/user/$UID_N/bus"
          fi

          out ""
          out "## AFTER"

          out ""
          out "### hooks — node tools/deploy-hooks.js --check --require-install   [GATING]"
          out '```'
          run "$NODE" "$WS/tools/deploy-hooks.js" --check --require-install
          s=$?
          out "exit $s"
          out '```'
          if [ "$s" -ne 0 ]; then
            echo "::error::hook drift is STILL present after running deploy-hooks.js"
            gate=1
          fi

          out ""
          out "### agents — node tools/sync-agents.js --check   [GATING]"
          out '```'
          run "$NODE" "$WS/tools/sync-agents.js" --check
          s=$?
          out "exit $s"
          out '```'
          if [ "$s" -ne 0 ]; then
            echo "::error::agent definition drift is STILL present after running sync-agents.js"
            gate=1
          fi

          out ""
          out "### timer — bash tools/install-brain-sync-timer.sh --check   [GATING]"
          out '```'
          run bash "$WS/tools/install-brain-sync-timer.sh" --check
          s=$?
          out "exit $s"
          out '```'
          if [ "$s" -ne 0 ]; then
            echo "::error::brain-sync timer drift is STILL present after running the installer"
            gate=1
          fi

          # Exit 4 from the installer means the units exist but nothing enabled them. Prove the
          # units really landed (--check-units skips the liveness half), then fail anyway. Reporting
          # success for a timer that is not scheduled is the exact false-green this mode exists to
          # end: on the host that matters, the 15-minute timer is the ONLY thing syncing the brain.
          if [ "${TIMER_RC:-0}" = "4" ]; then
            out ""
            out "### timer units on disk — --check-units (liveness half skipped)"
            out '```'
            run bash "$WS/tools/install-brain-sync-timer.sh" --check-units
            u=$?
            out "exit $u"
            out '```'
            if [ "$u" -ne 0 ]; then
              echo "::error::install-brain-sync-timer.sh exited 4 AND the units are not on disk — it wrote nothing usable"
            fi
            gate=1
            echo "::error::the memory sync timer is NOT running. The units were written to ~/.config/systemd/user/brain-sync.{service,timer} but the systemd --user bus was unreachable from the runner service context, so enable --now never happened. A human must run this at a console on $(hostname) as ${TIMER_USER:-the runner user}: loginctl enable-linger ${TIMER_USER:-<user>} && systemctl --user daemon-reload && systemctl --user enable --now brain-sync.timer"
            out ""
            out "HUMAN ACTION REQUIRED — run this at a console on $(hostname) as ${TIMER_USER:-the runner user}:"
            out '```'
            out "loginctl enable-linger ${TIMER_USER:-<user>} && systemctl --user daemon-reload && systemctl --user enable --now brain-sync.timer"
            out '```'
          fi

          # ── the off-Actions watchdog timer (#361) ────────────────────────────────────────────
          #
          # Run from the workspace, matching enforcement-drift-check.yml, and gating for the same
          # reason the brain-sync timer gates: this mode installed it, so this mode has to prove it.
          # --check asserts the units by MEANING (both ExecStart scripts on disk, SuccessExitStatus
          # intact, the timer repeats, gh can authenticate, the timer is active), which is safe
          # across the workspace/canon path difference and is strictly more than the daily job can
          # see. enforcement-drift-check.yml only observes this timer through the heartbeat
          # actions-watchdog.js stamps into ~/agent-memory, and "heartbeat missing or stale" is one
          # message covering four different faults (#361). A green here names which of them is
          # cleared; it is NOT itself a heartbeat, and the daily check stays the arbiter — the first
          # stamp only appears after the timer's OnBootSec=5min elapses, which is usually after this
          # job has finished.
          out ""
          out "### watchdog — bash tools/install-actions-watchdog.sh --check   [GATING]"
          out '```'
          run bash "$WS/tools/install-actions-watchdog.sh" --check
          s=$?
          out "exit $s"
          out '```'
          if [ "$s" -ne 0 ]; then
            echo "::error::actions-watchdog timer drift is STILL present after running the installer"
            gate=1
          fi

          # Exit 4: units exist, nothing enabled them. Prove the units really landed (--check-units
          # skips the liveness and gh halves), then fail anyway. Reporting success for a timer that
          # is not scheduled is the exact false-green this mode exists to end — and this timer is the
          # ONLY detector that survives a repo-level Actions disable (#197), so a silent one leaves
          # the whole repo with no liveness check at all.
          if [ "${WATCHDOG_RC:-0}" = "4" ]; then
            out ""
            out "### watchdog units on disk — --check-units (liveness half skipped)"
            out '```'
            run bash "$WS/tools/install-actions-watchdog.sh" --check-units
            u=$?
            out "exit $u"
            out '```'
            if [ "$u" -ne 0 ]; then
              echo "::error::install-actions-watchdog.sh exited 4 AND the units are not on disk — it wrote nothing usable"
            fi
            gate=1
            echo "::error::the actions-watchdog timer is NOT running. The units were written to ~/.config/systemd/user/actions-watchdog.{service,timer} but the systemd --user bus was unreachable from the runner service context, so enable --now never happened. A human must run this at a console on $(hostname) as ${TIMER_USER:-the runner user}: loginctl enable-linger ${TIMER_USER:-<user>} && systemctl --user daemon-reload && systemctl --user enable --now actions-watchdog.timer"
            out ""
            out "HUMAN ACTION REQUIRED — run this at a console on $(hostname) as ${TIMER_USER:-the runner user}:"
            out '```'
            out "loginctl enable-linger ${TIMER_USER:-<user>} && systemctl --user daemon-reload && systemctl --user enable --now actions-watchdog.timer"
            out '```'
          fi

          # Exit 5: gh is missing or unauthenticated for the runner user, so the installer wrote
          # nothing. Nothing dispatched from Actions can fix that — a token handed to this job dies
          # with it, and the unit runs later with only PATH and HOME. So make the failure actionable
          # rather than faking a green: name the exact console command. The daily
          # enforcement-drift-check keeps the #361 human-needed alert open until a heartbeat appears.
          if [ "${WATCHDOG_RC:-0}" = "5" ]; then
            gate=1
            echo "::error::the actions-watchdog timer could NOT be installed: gh is missing or unauthenticated for ${TIMER_USER:-the runner user} on $(hostname). Both watchdogs shell out to gh, so a timer installed anyway would fail every hour and stamp no heartbeat — indistinguishable from the fault this repair is meant to clear. A human must run this at a console: gh auth login   (then re-dispatch repair-install)"
            out ""
            out "HUMAN ACTION REQUIRED — run this at a console on $(hostname) as ${TIMER_USER:-the runner user}, then re-dispatch repair-install:"
            out '```'
            out "gh auth login"
            out '```'
          fi

          out ""
          out "### routines — node tools/routines.js verify   [INFORMATIONAL ONLY — does not gate]"
          out "repair-install cannot fix a routines/workflow mismatch; that is a repo-side code change."
          out '```'
          run "$NODE" "$WS/tools/routines.js" verify
          s=$?
          out "exit $s   (not gating)"
          out '```'

          out ""
          out "### ~/agent-memory was not touched"
          if git -C "$BRAIN" rev-parse HEAD >/dev/null 2>&1; then
            HEAD_A="$(git -C "$BRAIN" rev-parse HEAD)"
            DIRTY_A="$(git -C "$BRAIN" status --porcelain | wc -l | tr -d ' ')"
          else
            HEAD_A="none"
            DIRTY_A="n/a"
          fi
          out '```'
          out "HEAD  before ${BRAIN_HEAD_BEFORE:-<not recorded>}"
          out "HEAD  after  $HEAD_A"
          out "dirty before ${BRAIN_DIRTY_BEFORE:-<not recorded>}"
          out "dirty after  $DIRTY_A"
          out '```'
          # No step in this job writes, commits, syncs or pushes the brain. But one thing it does is
          # `systemctl --user enable --now brain-sync.timer`, and that timer carries OnBootSec=2min
          # against a host booted long ago — so systemd elapses it AT ONCE and brain-sync-run.js
          # commits and pushes while this job is still running. On the successful repair path a
          # moved HEAD is therefore the expected outcome, not an anomaly.
          #
          # Gating on it would make the first successful repair-install — the dispatch this mode was
          # built for — go red for working correctly, and a mode that is red when it succeeds gets
          # its failures ignored. So: always print the commits, and gate only when TIMER_RC != 0,
          # i.e. when no timer was enabled and no benign mover exists.
          if [ "${BRAIN_HEAD_BEFORE:-}" != "$HEAD_A" ]; then
            out ""
            out "commits that landed while this job ran:"
            out '```'
            git -C "$BRAIN" log --oneline "${BRAIN_HEAD_BEFORE:-HEAD}..$HEAD_A" 2>&1 | head -20 | tee -a "$GITHUB_STEP_SUMMARY"
            out '```'
            if [ "${TIMER_RC:-0}" = "0" ]; then
              out "EXPECTED: the timer this job enabled elapsed immediately (OnBootSec=2min, host booted long ago) and ran its first sync. Not a failure. The watchdog timer this job also enabled writes a heartbeat file into the brain on its own first run (#313), so that file appearing in these commits is expected too."
            else
              gate=1
              echo "::error::~/agent-memory HEAD moved during this job (${BRAIN_HEAD_BEFORE:-?} -> $HEAD_A) and TIMER_RC=${TIMER_RC:-?}, so the brain-sync timer was NOT enabled and cannot account for it. Nothing in this job writes the brain. Explain the commits above before trusting this run."
            fi
          fi

          out ""
          if [ "$gate" -eq 0 ]; then
            out "## VERDICT: PASS — hooks, agent definitions, the memory sync timer and the off-Actions watchdog timer are all in sync on $(hostname). This job wrote nothing into ~/agent-memory; if its HEAD moved, the block above names the commits and the timer that produced them."
          else
            out "## VERDICT: FAIL — see the ::error:: annotations above. Re-read BEFORE vs AFTER to see what the repair did and did not move."
          fi
          exit "$gate"

  # ── seed-action-cache ──────────────────────────────────────────────────────────────────────
  #
  # `Sam Security Audit` run 32182510133 failed in **Set up job**, before a single line of this
  # repo's code ran:
  #
  #     Failed to download action 'https://codeload.github.com/actions/checkout/tar.gz/11d5960a…'
  #     Error: Response status code does not indicate success: 429 (Too Many Requests).
  #     Failed to download archive '…' after 3 attempts.
  #
  # One PR sync fires six workflows here, and ~10 self-hosted jobs each re-download the identical
  # `actions/checkout` tarball from a single egress IP. codeload throttles the IP, `Set up job`
  # fails, and a required check goes red for a reason no diff can fix. Retrying is not a fix: the
  # next sync does exactly the same thing.
  #
  # The runner supports a local archive cache and this is the GitHub-supported knob. From
  # actions/runner `src/Runner.Common/Constants.cs`:
  #
  #     public static readonly string ActionArchiveCacheDirectory = "ACTIONS_RUNNER_ACTION_ARCHIVE_CACHE";
  #
  # and `src/Runner.Worker/ActionManager.cs`, which reads it and, on a hit, copies from disk
  # INSTEAD of calling codeload at all:
  #
  #     var cacheArchiveFile = Path.Combine(actionArchiveCacheDir,
  #         downloadInfo.ResolvedNameWithOwner.Replace(Path.DirectorySeparatorChar, '_')…,
  #         $"{downloadInfo.ResolvedSha}.tar.gz");            // .zip on Windows
  #     if (File.Exists(cacheArchiveFile)) { … File.Copy(cacheArchiveFile, archiveFile); useActionArchiveCache = true; }
  #     …
  #     if (!useActionArchiveCache) { await DownloadRepositoryArchive(…); }
  #
  # Two properties of that code shape this job. A **miss falls straight through to the download**,
  # so a partial or stale cache can only help, never break a run — which is why this job needs no
  # rollback path. And the layout is fixed: `<cache>/<owner>_<repo>/<sha>.tar.gz`, keyed on the
  # RESOLVED SHA, which is why every `uses:` in this repo being SHA-pinned is what makes seeding
  # possible at all — a tag-pinned action resolves to a SHA the runner only learns at job time.
  #
  # ── Why a separate mode and not part of repair-install ─────────────────────────────────────
  #
  # repair-install's preflight `fatal`s when the canonical checkout is unhealthy, and rightly so.
  # But the 429 has nothing to do with ~/agent-memory or brain-sync, and a fix for a red required
  # check must not be blocked behind an unrelated brain repair. This mode reads no brain state,
  # writes no repo state, and can run when repair-install cannot.
  #
  # ── Why this job never restarts the runner ─────────────────────────────────────────────────
  #
  # It is EXECUTING ON the runner it would be restarting; `svc.sh restart` here kills this job
  # mid-step and reports a failure — one more red run, which is the thing being fixed. So the job
  # seeds the cache and writes the export, then REPORTS whether the variable is live in the
  # listener process rather than claiming a result it cannot produce. The runner self-updates and
  # restarts on its own cadence, so the setting goes live without a human; the job says so plainly
  # instead of pretending it is already in effect.
  seed-action-cache:
    if: inputs.mode == 'seed-action-cache'
    runs-on: [self-hosted, Linux]
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Seed the action archive cache
        shell: bash
        run: |
          # Diagnostic-and-best-effort, same contract as `status`: GitHub invokes this as
          # `bash -e -o pipefail`, and one unreachable tarball must not abort the seeding of the
          # rest. Every fallible command carries its own rc.
          set +e
          set -uo pipefail
          WS="$GITHUB_WORKSPACE"
          CACHE="$HOME/actions-runner-action-cache"

          out() { printf '%s\n' "$*" | tee -a "$GITHUB_STEP_SUMMARY"; }

          out "## Action archive cache on $(hostname)"
          out ""
          out '```'
          out "cache dir     $CACHE"
          if ! mkdir -p "$CACHE"; then
            out "FATAL - cannot create $CACHE"
            out '```'
            exit 1
          fi

          # Collect every SHA-pinned `uses:` in this repo. Deliberately parsed out of the workflow
          # files rather than hard-coded: a list that has to be updated by hand is a list that goes
          # stale silently, and a stale entry here reads as a working cache that never hits.
          PINS="$(grep -rhoE 'uses:[[:space:]]+[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+@[0-9a-f]{40}' "$WS/.github/workflows/" | sed -E 's/^uses:[[:space:]]+//' | sort -u)"
          out "pinned actions $(printf '%s\n' "$PINS" | grep -c .)"
          out '```'
          out ""
          out '```'

          SEEDED=0
          PRESENT=0
          FAILED=0
          while IFS= read -r pin; do
            [ -n "$pin" ] || continue
            REPO_PART="${pin%@*}"
            SHA="${pin#*@}"
            OWNER="${REPO_PART%%/*}"
            NAME="${REPO_PART#*/}"
            # Layout is fixed by ActionManager.cs: '/' in owner/repo becomes '_', file is <sha>.tar.gz.
            DIR="$CACHE/${OWNER}_${NAME}"
            DEST="$DIR/${SHA}.tar.gz"
            if [ -s "$DEST" ]; then
              out "present  $pin"
              PRESENT=$((PRESENT+1))
              continue
            fi
            mkdir -p "$DIR"
            # Download ONCE, here, instead of once per job forever. Straight to a temp name so an
            # interrupted transfer can never leave a truncated file that the runner would happily
            # copy and then fail to untar.
            curl -fsSL --retry 3 --retry-delay 5 -o "$DEST.part" "https://codeload.github.com/${OWNER}/${NAME}/tar.gz/${SHA}"
            CURL_RC=$?
            if [ $CURL_RC -eq 0 ] && tar -tzf "$DEST.part" >/dev/null 2>&1; then
              mv -f "$DEST.part" "$DEST"
              out "seeded   $pin"
              SEEDED=$((SEEDED+1))
            else
              rm -f "$DEST.part"
              out "FAILED   $pin (curl rc $CURL_RC; left uncached, runner falls through to codeload)"
              FAILED=$((FAILED+1))
            fi
          done <<< "$PINS"
          out '```'
          out ""
          out "seeded $SEEDED, already present $PRESENT, failed $FAILED"

      - name: Wire the cache into the runner service, then report whether it is live
        shell: bash
        run: |
          set +e
          set -uo pipefail
          CACHE="$HOME/actions-runner-action-cache"
          RUNNER_DIR="$HOME/actions-runner"
          SVC="$RUNNER_DIR/runsvc.sh"

          out() { printf '%s\n' "$*" | tee -a "$GITHUB_STEP_SUMMARY"; }

          out ""
          out "### Wiring ACTIONS_RUNNER_ACTION_ARCHIVE_CACHE into the service"
          out '```'
          out "runner dir    $RUNNER_DIR"

          # runsvc.sh, NOT a systemd drop-in and NOT a `.env` file. The unit generated by the
          # runner's own svc.sh is `ExecStart={{RunnerRoot}}/runsvc.sh` with no EnvironmentFile, so
          # a `.env` would be read by nothing; a drop-in would need sudo, which this job does not
          # have and should not want on the host that gates merges. runsvc.sh is owned by the
          # runner user and carries an explicit insertion point for exactly this:
          #   "# insert anything to setup env when running as a service"
          if [ ! -f "$SVC" ]; then
            out "runsvc.sh     NOT FOUND at $SVC - cannot wire the variable."
            out "              The cache directory is still seeded and costs nothing; set"
            out "              ACTIONS_RUNNER_ACTION_ARCHIVE_CACHE=$CACHE by hand for the service."
            out '```'
            exit 0
          fi

          MARKER='insert anything to setup env when running as a service'
          if grep -qF 'ACTIONS_RUNNER_ACTION_ARCHIVE_CACHE' "$SVC"; then
            out "runsvc.sh     already exports the variable - left untouched"
          elif ! grep -qF "$MARKER" "$SVC"; then
            out "runsvc.sh     insertion marker NOT FOUND - not edited."
            out "              Add by hand, before the RunnerService.js launch:"
            out "              export ACTIONS_RUNNER_ACTION_ARCHIVE_CACHE=$CACHE"
          else
            cp -p "$SVC" "$SVC.bak.$(date -u +%Y%m%dT%H%M%SZ)"
            # Insert immediately after the marked line so the export precedes the RunnerService
            # launch. Appending at EOF would land AFTER `wait $PID` and never execute.
            awk -v cache="$CACHE" -v marker="$MARKER" '
              { print }
              index($0, marker) && !done { print "export ACTIONS_RUNNER_ACTION_ARCHIVE_CACHE=\"" cache "\""; done = 1 }
            ' "$SVC" > "$SVC.new"
            if grep -qF 'ACTIONS_RUNNER_ACTION_ARCHIVE_CACHE' "$SVC.new"; then
              cat "$SVC.new" > "$SVC"   # preserve the existing inode, mode and ownership
              rm -f "$SVC.new"
              out "runsvc.sh     export inserted (a timestamped .bak sits beside it)"
            else
              rm -f "$SVC.new"
              out "runsvc.sh     rewrite produced no export - left untouched"
            fi
          fi

          # Is it live RIGHT NOW? The seeding above does nothing until the listener process itself
          # carries the variable, and "the file says so" is not "the process has it" - the same
          # distinction #362 turned on. Read the actual process environment.
          LISTENER_PID="$(pgrep -f Runner.Listener | head -1)"
          if [ -n "$LISTENER_PID" ] && [ -r "/proc/$LISTENER_PID/environ" ]; then
            if tr '\0' '\n' < "/proc/$LISTENER_PID/environ" | grep -q '^ACTIONS_RUNNER_ACTION_ARCHIVE_CACHE='; then
              out "LIVE          YES - the running listener carries it; cache hits start now"
            else
              out "LIVE          NOT YET - the running listener predates this edit."
              out "              This job will NOT restart the runner: it is executing ON that"
              out "              runner, so a restart kills this job and reports a failure. The"
              out "              setting goes live at the next runner restart, which the runner's"
              out "              own auto-update performs unattended. To force it from the console:"
              out "                cd $RUNNER_DIR && sudo ./svc.sh stop && sudo ./svc.sh start"
            fi
          else
            out "LIVE          UNKNOWN - no readable Runner.Listener process environment"
          fi
          out '```'
          out ""
          out "_A cache MISS is harmless: ActionManager.cs falls through to the normal download, so"
          out "a partial or stale cache can only reduce codeload traffic, never break a job._"

  # ── heal-canon ─────────────────────────────────────────────────────────────────────────────
  #
  # The gap between `status` and `repair-install`, and the last source of the nightly failure
  # email (#462). `enforcement-drift-check` reports:
  #
  #     FAILED: canon checkout is behind origin/main
  #
  # and repair-install's bounded self-heal answers, correctly:
  #
  #     SKIPPED — canon has 2 dirty path(s); someone may be mid-edit.
  #     Rewriting that tree from a dispatch is not acceptable.
  #
  # Both are right. `repair-install` may never start rewriting working trees as a side effect of
  # someone dispatching it to fix hooks, so that skip stays exactly as it is. But that leaves the
  # host with no repair path at all, and `ssh` here is refused, so the drift has stood since
  # 2026-08-10 failing a nightly job nobody could act on.
  #
  # This mode is the missing path, and it is a separate dispatch precisely so that touching a
  # dirty tree is always something a human chose, never something a hook repair did on the way
  # past. Its contract is narrow: PRESERVE the edits, never adjudicate them.
  #
  #   * `git stash push` — the only mutation of the working tree. NOT `checkout --`, NOT
  #     `reset --hard`, NOT `clean`. Those three are banned here the same way they are banned in
  #     repair-brain and reclone-brain: each destroys work with a zero exit code, and a repair
  #     that can silently lose a human's edits is worse than the drift it fixes.
  #   * The stash is PROVEN to exist before the tree is touched further — `refs/stash` resolves,
  #     the entry carrying this run's stamp is in `git stash list`, and the stash count went up.
  #     Same shape, and the same reason, as the reclone-brain prove step: an unverified "it
  #     worked" from the step that is supposed to be preserving the data is the one failure that
  #     cannot be undone afterwards. If any of the three proofs fails, the run STOPS and does
  #     nothing else — no fetch, no merge, no second attempt.
  #   * UNTRACKED files stop the run before anything happens at all. `stash push` without `-u`
  #     leaves them where they are, so they cannot be swept away by this job — but merging on top
  #     of them can fail in confusing ways, and more to the point a human deciding keep-or-discard
  #     must be shown them rather than have them silently excluded from the stash they are told
  #     preserves their work.
  #   * A tree with no local edits is not this mode's job — it is repair-install's, which already
  #     handles the clean case and does the branch-return as well. Reported and exited 0.
  #
  # Nothing here reads or writes ~/agent-memory. This is code only.
  heal-canon:
    if: inputs.mode == 'heal-canon'
    runs-on: [self-hosted, Linux]
    permissions:
      contents: read
    steps:
      - name: Stash canon's local edits, prove the stash exists, then fast-forward
        shell: bash
        run: |
          # `set +e` first and deliberately (#460): GitHub invokes this as `bash --noprofile
          # --norc -e -o pipefail {0}`, and a step's own `set -uo pipefail` does NOT clear that
          # -e. Every guard below is "run it, capture $?, then decide" — a SIMPLE COMMAND, which
          # under the inherited -e kills the step before the `$?` line and before `stop` can
          # print why. In this job that would be catastrophic rather than merely confusing: the
          # step would die mid-sequence with no statement of what it had already done to the
          # tree. Nothing below relies on -e; every mutation is followed by an explicit check.
          set +e
          set -uo pipefail
          CANON="$HOME/dev/AgentSystem"

          out() { printf '%s\n' "$*" | tee -a "$GITHUB_STEP_SUMMARY"; }
          stop() {
            out ""
            out "**STOPPING.** $*"
            echo "::error::$*"
            exit 1
          }

          out "## heal-canon on \`$(hostname)\`"
          out ""
          out '```'
          out "canon   $CANON"

          [ -e "$CANON" ] || stop "$CANON does not exist on this host — there is nothing to heal. Clone the repo there and dispatch repair-install."
          git -C "$CANON" rev-parse --git-dir >/dev/null 2>&1 \
            || stop "$CANON exists but is not a git checkout. This job will not guess what it is; inspect it on the host."

          BRANCH="$(git -C "$CANON" rev-parse --abbrev-ref HEAD 2>/dev/null || echo '?')"
          HEAD_BEFORE="$(git -C "$CANON" log -1 --format='%h %ad %s' --date=iso 2>/dev/null || echo '?')"
          SHA_BEFORE="$(git -C "$CANON" rev-parse HEAD 2>/dev/null || echo '?')"
          out "branch  $BRANCH"
          out "HEAD    $HEAD_BEFORE"
          out '```'

          [ "$BRANCH" != "HEAD" ] && [ "$BRANCH" != "?" ] \
            || stop "canon is on a DETACHED HEAD. Stashing from a detached HEAD leaves the stash reachable but the branch context unrecoverable, and this job will not do that. On the host: git -C $CANON switch -c <name>, then re-dispatch."

          # ── untracked files: name them and stop, before anything is touched ──────────────────
          UNTRACKED_FILE="$(mktemp)"
          git -C "$CANON" ls-files --others --exclude-standard > "$UNTRACKED_FILE" 2>&1
          if [ -s "$UNTRACKED_FILE" ]; then
            out ""
            out "Untracked files present in canon — these would NOT be captured by \`git stash push\`:"
            out '```'
            head -50 "$UNTRACKED_FILE" | tee -a "$GITHUB_STEP_SUMMARY"
            out '```'
            stop "canon holds untracked file(s), listed above. This job stashes TRACKED edits only and will not sweep untracked files up silently, nor claim to have preserved work it did not touch. Deal with them on the host (keep, delete, or commit them), then re-dispatch heal-canon."
          fi

          # ── nothing to preserve is repair-install's case, not this one ───────────────────────
          DIRTY="$(git -C "$CANON" status --porcelain 2>/dev/null | wc -l | tr -d ' ')"
          if [ "$DIRTY" = "0" ]; then
            out ""
            out "canon has NO local edits — there is nothing for this mode to preserve."
            out "Dispatch \`repair-install\`: its bounded self-heal already handles the clean case"
            out "(fetch, return to \`main\` if parked elsewhere, then merge --ff-only)."
            exit 0
          fi

          out ""
          out "### edits about to be stashed ($DIRTY dirty path(s))"
          out '```'
          git -C "$CANON" status --porcelain 2>&1 | head -50 | tee -a "$GITHUB_STEP_SUMMARY"
          out '```'
          out '```'
          git -C "$CANON" diff --stat 2>&1 | head -50 | tee -a "$GITHUB_STEP_SUMMARY"
          out '```'

          # ── stash, then PROVE it before anything else moves ──────────────────────────────────
          STAMP="$(date -u +%Y%m%dT%H%M%SZ)"
          STASH_MSG="canon-heal $STAMP"
          STASH_COUNT_BEFORE="$(git -C "$CANON" stash list 2>/dev/null | wc -l | tr -d ' ')"

          STASH_OUT="$(git -C "$CANON" stash push -m "$STASH_MSG" 2>&1)"
          STASH_RC=$?
          out ""
          out "### git stash push -m \"$STASH_MSG\""
          out '```'
          [ -n "$STASH_OUT" ] && out "$STASH_OUT"
          out "exit $STASH_RC"
          out '```'

          # Three independent proofs, all required. A non-zero exit is the easy failure; the one
          # that matters is a ZERO exit with no stash behind it, because that is the shape that
          # would let the merge proceed over edits nothing is holding.
          STASH_SHA="$(git -C "$CANON" rev-parse --verify --quiet refs/stash 2>/dev/null)"
          STASH_ENTRY="$(git -C "$CANON" stash list --format='%gd %gs' 2>/dev/null | grep -F "$STASH_MSG" | head -1)"
          STASH_COUNT_AFTER="$(git -C "$CANON" stash list 2>/dev/null | wc -l | tr -d ' ')"

          out '```'
          out "refs/stash resolves to   ${STASH_SHA:-<nothing>}"
          out "entry for this run       ${STASH_ENTRY:-<not found in git stash list>}"
          out "stash count              $STASH_COUNT_BEFORE -> $STASH_COUNT_AFTER"
          out '```'

          [ "$STASH_RC" -eq 0 ] \
            || stop "git stash push exited $STASH_RC. The working tree was NOT touched further — no fetch and no merge were run. Read the git output above; the edits are still in the tree exactly as they were."
          [ -n "$STASH_SHA" ] \
            || stop "git stash push reported success but refs/stash does not resolve — there is no stash object holding those edits, so nothing here has preserved them and this job will not merge over them. Nothing further was done."
          [ -n "$STASH_ENTRY" ] \
            || stop "refs/stash resolves but no entry carrying \"$STASH_MSG\" is in git stash list — this run cannot prove the stash it is about to rely on is the one it created. Refusing to merge over edits it cannot name. Nothing further was done."
          [ "$STASH_COUNT_AFTER" -gt "$STASH_COUNT_BEFORE" ] \
            || stop "the stash count did not increase ($STASH_COUNT_BEFORE -> $STASH_COUNT_AFTER), so no new stash entry was created despite a zero exit. Refusing to merge. Nothing further was done."

          STASH_REF="${STASH_ENTRY%% *}"
          POP_CMD="git -C $CANON stash pop '$STASH_REF'"

          # The tree must actually be clean now; if it is not, the merge would touch edits the
          # stash does not hold, which is the whole thing this job exists not to do.
          DIRTY_AFTER="$(git -C "$CANON" status --porcelain 2>/dev/null | wc -l | tr -d ' ')"
          if [ "$DIRTY_AFTER" != "0" ]; then
            git -C "$CANON" status --porcelain 2>&1 | head -20 | tee -a "$GITHUB_STEP_SUMMARY"
            stop "canon still reports $DIRTY_AFTER dirty path(s) after the stash, listed above. No fetch and no merge were run. Your edits are safe in $STASH_REF ($STASH_SHA); restore them with: $POP_CMD"
          fi

          out ""
          out "PROVEN: edits preserved in \`$STASH_REF\` (\`$STASH_SHA\`), canon tree now clean."

          # ── fetch + merge --ff-only ──────────────────────────────────────────────────────────
          # --ff-only and nothing else: if canon holds commits origin/main does not, this refuses
          # rather than merging or rebasing, and the stash is still there to pop.
          out ""
          out "### fetch + merge --ff-only origin/main"
          out '```'
          FETCH_OUT="$(git -C "$CANON" fetch --quiet origin main 2>&1)"
          FETCH_RC=$?
          [ -n "$FETCH_OUT" ] && out "$FETCH_OUT"
          [ "$FETCH_RC" -eq 0 ] \
            || stop "git fetch origin main failed in canon (exit $FETCH_RC) — no network, no credentials, or origin unreachable. Canon is unchanged apart from the stash; restore your edits with: $POP_CMD"

          if [ "$BRANCH" != "main" ]; then
            out "canon is on branch '$BRANCH', not main."
            out '```'
            out ""
            out "The tree is now clean, which is the state \`repair-install\` needs: dispatch it to"
            out "return canon to \`main\` and fast-forward. Returning canon to another branch is its"
            out "job, not this one's, and doing it here would duplicate that logic untested."
            out ""
            out "Your edits are preserved. Restore them with:"
            out '```'
            out "$POP_CMD"
            out '```'
            exit 0
          fi

          MERGE_OUT="$(git -C "$CANON" merge --ff-only origin/main 2>&1)"
          MERGE_RC=$?
          [ -n "$MERGE_OUT" ] && out "$MERGE_OUT"
          out "exit $MERGE_RC"
          out "HEAD before  $HEAD_BEFORE"
          out "HEAD after   $(git -C "$CANON" log -1 --format='%h %ad %s' --date=iso 2>/dev/null || echo '?')"
          out '```'

          [ "$MERGE_RC" -eq 0 ] \
            || stop "git merge --ff-only origin/main refused (exit $MERGE_RC) — canon holds commits that are not on origin/main, so it cannot fast-forward, and NOTHING was forced. Canon is still at $SHA_BEFORE. Your edits are preserved; restore them with: $POP_CMD"

          out ""
          out "### DONE — canon fast-forwarded, your edits preserved"
          out ""
          out "The edits that were in the tree are in the stash, untouched. Restore them with:"
          out ""
          out '```'
          out "$POP_CMD"
          out '```'
          out ""
          out "Read them without restoring: \`git -C $CANON stash show -p '$STASH_REF'\`."
          out "If the fast-forward makes the pop conflict, \`git -C $CANON stash branch <name> '$STASH_REF'\`"
          out "checks the stash out on a branch at the commit it was made from and applies it there."
          out "Nothing in this job deletes a stash; \`$STASH_REF\` stays until a human drops it."
          exit 0
