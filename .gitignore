.agents/sync.log
nexus/agentsystem/
nexus/*/
node_modules/
.claude/

# AgentSystem local graph brain (decaying memory — not committed)
nexus/

# Untracked runtime files
HANDOFF.md
.claudedaemon
.worktrees/

# Personal life-OS skills — kept private, never published
/skills/daily-briefing/
/skills/daily-triage/
/skills/inbox-capture/
/skills/weekly-review/

# SSH keypair for the Mission Control server — never commit a private key
/linux-server
/linux-server.pub

# Deleted in #213: agent scratch logs quoting a client's private issue numbers, compliance gates,
# and infra details — in a public repo. Ignored so they cannot re-enter by accident. Purging them
# from git history is tracked in #214.
/docs/archive/agents-memory/
