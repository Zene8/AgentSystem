# Mission Control self-update schedule — runs auto-update once a day.
# Persistent=true catches up if the box was off at the scheduled time.
[Unit]
Description=Daily Mission Control self-update

[Timer]
OnCalendar=*-*-* 04:00:00
RandomizedDelaySec=1800
Persistent=true

[Install]
WantedBy=timers.target
